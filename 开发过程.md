# Paper Research Copilot 开发过程

- 最后更新：`2026-08-19`
- 当前阶段：`Corpus v3 端到端 Evaluation 完成，生产演示已切换 v3`
- 当前单论文 Discovery：`Dense + BM25 + RRF`
- 当前跨论文 Retrieval：`Query Decomposition + Hybrid RRF + Coverage Merge`

## 1. 文档目的

本文档用于持续记录项目的主要开发思路、技术决策和优化过程，帮助后续复盘、准备面试和
整理简历材料。

后续每次关键改动都应至少记录以下内容：

1. 原有设计和暴露的问题。
2. 为什么需要优化，以及希望改善哪个指标。
3. 采用了什么措施，为什么选择该方案。
4. 使用什么数据集、参数和指标进行验证。
5. 优化后的提升、退化和适用边界。
6. 是否替换默认方案，以及下一步工作。

不能只记录“实现了什么”，也不能只展示最好的结果。出现指标退化时，应同时记录原因和
最终取舍。

## 2. 项目定位

项目名称：`Paper Research Copilot`

目标是实现一个能够自主规划论文研究任务、进行多策略检索、分析论文内容、验证引用并生成
结构化研究报告的 Agentic RAG 系统，重点体现：

- RAG 深度。
- Agent 设计和工程实现。
- Evaluation。
- 工程化能力。

为了避免过早引入 Agent 导致底层问题无法定位，开发顺序采用：

```text
先跑通基础 RAG
  -> 建立可观察性
  -> 建立版本化语料
  -> 建立 Retriever Evaluation
  -> 用实验驱动检索优化
  -> 再引入 Planner、Research Agent 和 Reflection
```

当前阶段还没有 Planning、Tools、Memory、Agent Runtime 和自动 Query Routing。当前重点是
建立可验证的 RAG 基础设施。

## 3. 第一阶段：跑通最小 RAG 主链路

### 3.1 开发原因

后续所有 Agent 行为都依赖 PDF 解析、Chunk、Embedding、检索和引用链路。如果基础 RAG
不能稳定运行，Agent 只会放大底层错误，而且很难判断问题来自模型、检索还是编排。

因此第一个版本不引入 LangChain、LangGraph 或 Agent，先独立跑通：

```text
PDF
  -> Parse
  -> Chunk
  -> Metadata
  -> Embedding
  -> Qdrant
  -> Dense Retriever
  -> Prompt
  -> LLM Answer
  -> Citations
```

### 3.2 主要措施

- 使用 `pypdf` 按页抽取论文文本。
- Chunk 不跨页，保证引用可以定位到 PDF 页码。
- 每个 Chunk 保存稳定 ID、论文 ID、页码、Section、字符位置、Corpus 和 Chunking 版本。
- 使用 SiliconFlow 的 `BAAI/bge-m3` 生成 Embedding。
- 使用 Qdrant 保存 Chunk 和向量。
- 使用 OpenAI-compatible 接口调用 LLM。
- Prompt 要求回答只能依据检索 Evidence，并通过 `C1`、`C2` 等 Citation ID 引用来源。

### 3.3 环境决策

- 项目使用独立 Conda 环境：`paper-research-copilot`。
- 所有项目命令都通过该环境运行，不使用系统 Python。
- `.env` 固定从项目根目录读取，解决从其他工作目录启动 CLI 时无法读取 API 配置的问题。
- `.env` 包含真实密钥，不进入 Git。

### 3.4 阶段结果

最小链路已经能够完成 PDF 导入、向量检索、Prompt 构造、答案生成和 Citation 输出。

这一阶段证明的是：

> 基础链路可以真实运行，但还不能证明检索质量足够好，也不能证明系统具有 Agent 能力。

## 4. 第二阶段：增加可观察性

### 4.1 优化原因

只查看最终答案无法定位问题。例如答案错误可能来自：

- PDF 解析乱码。
- Chunk 边界不合理。
- Metadata 缺失。
- Retriever 没召回正确内容。
- Prompt 丢失上下文。
- LLM 没有正确使用 Evidence。

因此需要把主链路拆开观察。

### 4.2 优化措施

增加四个调试命令：

```powershell
paper-rag inspect-pdf .\data\papers\react.pdf
paper-rag inspect-chunks .\data\papers\react.pdf
paper-rag search "ReAct 如何使用外部工具？" --top-k 5
paper-rag show-prompt "ReAct 如何使用外部工具？" --top-k 5
```

它们分别用于检查：

| 命令 | 观察内容 |
| --- | --- |
| `inspect-pdf` | PDF Metadata、页数、每页字符数和文本预览 |
| `inspect-chunks` | Chunk 页码、长度、Section、稳定 ID 和原文 |
| `search` | Dense Top-K、分数、论文、页码和完整 Evidence |
| `show-prompt` | 真正发送给 LLM 的 System Prompt 和 User Prompt |

### 4.3 优化效果

主链路中的 Parse、Chunk、Retrieval、Prompt 和 Answer 可以分别检查。后续 Retriever 优化
不再依赖观察最终答案，而可以直接用检索结果和确定性指标验证。

## 5. 第三阶段：建立版本化种子语料库

### 5.1 优化原因

只使用一篇 ReAct 论文无法测试：

- 跨论文召回。
- 相似主题论文之间的干扰。
- 论文级去重。
- 多来源引用。
- Retriever 是否被某一篇论文占满。

直接下载大量论文也不合适，因为语料质量、版本和 Parse 状态不可控，实验难以复现。

### 5.2 优化措施

建立 `agent-seed-v1`，选择 10 篇具有代表性的 Agent 论文：

```text
ReAct
Toolformer
Reflexion
Language Agent Tree Search
MemGPT
Generative Agents
Voyager
CAMEL
AutoGen
AgentBench
```

语料库记录：

- 固定的 arXiv 版本和下载地址。
- 文件 SHA-256。
- 论文 ID、slug、标题和主题。
- PDF 页数和文本抽取统计。
- Parse Warning 和人工视觉检查结果。
- Corpus、Embedding、Chunking 和 Collection 版本。

### 5.3 Chunking v1

当前 Chunk 策略：

```text
chunk_size: 3200 characters
overlap: 400 characters
段落优先
不跨页
Section heuristic
稳定 Chunk ID
```

不跨页是为了保证 Citation 可定位；稳定 Chunk ID 和版本 Metadata 用于保证重复导入幂等。

### 5.4 阶段结果

```text
Corpus: agent-seed-v1
Papers: 10
Pages: 347
Chunks: 571
Embedding: BAAI/bge-m3
Collection: agent_seed_v1_bge_m3_chunking_v1
```

Qdrant 完整导入后为 571 个点，重复导入后仍为 571 个点，证明 Corpus Ingestion 具备幂等性。

## 6. 第四阶段：建立 Dense Retriever Evaluation

### 6.1 优化原因

此前只能通过人工查看 `search` 输出判断检索效果，无法量化不同策略之间的提升和退化。

### 6.2 优化措施

建立 `retrieval_diagnostic_v1`，包含 25 条问题，并标注目标论文、参考页码、问题类型、难度
和 Evidence Hint。

引入以下指标：

| Metric | 含义 |
| --- | --- |
| Paper Recall@K | Top-K 覆盖目标论文的比例 |
| Complete Paper Coverage@K | 是否覆盖全部目标论文 |
| Exact Page Recall@K | 是否召回目标论文的标注页 |
| Paper / Page MRR | 第一个正确结果的排名质量 |
| Same-page Redundancy | 同一论文同一页重复占用 Top-K 的比例 |

当前参考页码不是全部相关页的穷举，因此 Exact Page Recall 是严格下界。在没有完整
Chunk-level relevance label 前，不报告 Precision@K 和 nDCG。

### 6.3 Dense v1 结果

| K | Paper Recall | Complete Papers | Exact Page Recall | Same-page Redundancy |
| ---: | ---: | ---: | ---: | ---: |
| 1 | 86% | 76% | 62% | 0% |
| 3 | 98% | 96% | 80% | 8% |
| 5 | 98% | 96% | 82% | 10.4% |
| 10 | 98% | 96% | 86% | 19.6% |

该结果最初看起来较好，但进一步检查发现数据集存在 Title Leakage。

## 7. 第一次 Evaluation 优化：消除 Title Leakage

### 7.1 优化原因

原来的 25 个问题全部直接包含论文名，例如：

```text
ReAct 如何结合推理与行动？
CAMEL 与 AutoGen 有什么区别？
```

这种问题已经告诉 Retriever 应该查找哪篇论文，因此高 Paper Recall 主要反映：

```text
已知论文 -> 论文内定位
```

它不能证明系统能够根据技术概念发现正确论文。

### 7.2 优化措施

新增 `retrieval_discovery_v1`：

- 25 条不包含目标论文名称的问题。
- 20 条单论文发现问题。
- 5 条跨论文 Hard Negative。
- 保留目标论文和参考页码标注。
- Dataset Schema 区分 `known_paper` 和 `discovery`。
- 加载数据时根据 Corpus 的标题、简称和 slug 自动检查目标论文名称泄漏。

例如：

```text
哪种方法通过交错生成 Thought、Action 和 Observation，
让语言模型同时推理并操作环境？
```

### 7.3 优化效果

现在两个数据集承担不同职责：

| Dataset | 作用 |
| --- | --- |
| `retrieval_diagnostic_v1` | 已知论文条件下的 Evidence 定位 |
| `retrieval_discovery_v1` | 自然问题下的论文发现和跨论文覆盖 |

Discovery Dense Baseline 的 Top-10 结果：

```text
Paper Recall: 98%
Complete Paper Coverage: 96%
Cross-paper Paper Recall: 90%
Cross-paper Complete Coverage: 80%
Exact Page Recall: 96%
Same-page Redundancy: 13.6%
```

它暴露出 Dense Top-K 可能被单一高相似论文占满的问题。

## 8. 第一次 Retriever 优化：Diversified Dense

### 8.1 优化原因

跨论文问题需要同时召回多篇论文，但原始 Dense 只按全局相似度排序。某一篇高相似论文的
多个 Chunk 可能占满 Top-K，使另一篇目标论文无法进入结果。

### 8.2 优化措施

实现 Paper/Page constrained Dense Retrieval：

```text
Query Embedding
  -> Dense Top-50
  -> 按原相似度顺序遍历
  -> 同一论文同一页最多 1 个 Chunk
  -> 单篇论文最多 3 个 Chunk
  -> 返回 Top-10
```

该策略只使用 Metadata 约束，没有计算 Chunk 之间的语义相似度，因此不能称为 MMR。

### 8.3 优化效果

Discovery Top-10：

| Strategy | Paper Recall | Cross-paper Complete | Exact Page Recall | Same-page Redundancy |
| --- | ---: | ---: | ---: | ---: |
| Dense | 98% | 80% | 96% | 13.6% |
| Diversified Dense，paper=3 | 100% | 100% | 86% | 0% |
| Diversified Dense，paper=4 | 98% | 80% | 92% | 0% |

`paper=3` 修复了跨论文覆盖，但硬性数量限制会在正确页码出现前截断同一论文的后续候选，
使 Exact Page Recall 从 96% 降到 86%。`paper=4` 减少了页码损失，但重新失去跨论文覆盖
收益。

### 8.4 阶段结论

Diversified Dense 证明了“单一来源占满 Top-K”确实是问题，但它属于诊断性、可解释的
Baseline，不适合直接全面替代 Dense。

## 9. 第二次 Retriever 优化：MMR Dense

### 9.1 优化原因

规则版 Diversified Dense 的单论文上限过于刚性，无法区分：

- 同一论文中的重复内容。
- 同一论文中的互补证据。
- 不同论文中语义相似但比较时都必须保留的内容。

因此需要验证基于语义多样性的 MMR 是否能以更小的证据定位损失改善结果多样性。

### 9.2 优化措施

实现标准 Maximal Marginal Relevance：

```text
MMR Score
= lambda * Query relevance
- (1 - lambda) * 与已选 Chunk 的最大余弦相似度
```

实现细节：

- 每个问题只生成一次 Query Embedding。
- 从 Qdrant 读取 Dense Top-50 及其已保存向量，不重新 Embedding 候选 Chunk。
- 第一个结果保持 Dense Top-1。
- 后续结果在相关性和语义新颖性之间平衡。
- 使用 NumPy 矩阵运算计算候选相似度。
- 保留原始 Dense score，只改变候选选择顺序。
- 重新生成连续 Citation ID。

最初使用纯 Python 逐对计算时 P50 约为 900 ms。改为 NumPy 向量化后，P50 降至约
254-271 ms，证明该延迟主要来自实现方式，而不是 MMR 算法本身。

### 9.3 Lambda Sweep：Discovery 数据集

除 Lambda 外，Dataset、Candidate Pool、Embedding、Collection 和 Top-K 完全相同。

| Strategy | Paper Recall@10 | Cross-paper Complete@10 | Exact Page@5 | Exact Page@10 | Same-page Redundancy@10 | P50 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| Dense | 98% | 80% | 90% | 96% | 13.6% | 230 ms |
| Diversified Dense | 100% | 100% | 86% | 86% | 0% | 238 ms |
| MMR，lambda=0.50 | 100% | 100% | 66% | 82% | 0.4% | 265 ms |
| MMR，lambda=0.70 | 100% | 100% | 82% | 82% | 2.0% | 271 ms |
| MMR，lambda=0.85 | 100% | 100% | 86% | 94% | 6.8% | 254 ms |

结果解释：

- `lambda=0.50` 过度强调语义新颖性，导致高相关互补证据被排除。
- `lambda=0.70` 有所改善，但 Exact Page@10 仍只有 82%。
- `lambda=0.85` 更重视 Query relevance，在保持 100% 论文覆盖的同时，把 Exact Page@10
  提升到 94%。
- MMR 三组实验的 Cross-paper Exact Page Recall 都是 70%，仍低于 Dense 的 80%；总体
  指标提升不代表每个子集都提升。

### 9.4 已知论文集交叉验证

| Strategy | Paper Recall@10 | Cross-paper Complete@10 | Exact Page@5 | Exact Page@10 | Same-page Redundancy@10 |
| --- | ---: | ---: | ---: | ---: | ---: |
| Dense | 98% | 80% | 82% | 86% | 19.6% |
| MMR，lambda=0.50 | 100% | 100% | 74% | 86% | 1.2% |
| MMR，lambda=0.70 | 100% | 100% | 78% | 82% | 8.0% |
| MMR，lambda=0.85 | 98% | 80% | 82% | 84% | 16.4% |

`lambda=0.85` 在已知论文定位集上没有超过 Dense，说明它不能作为全局默认 Retriever。
Lambda 必须与检索目标绑定，不能只选择在单个数据集上最好的结果。

### 9.5 阶段结论

当前策略决策：

```text
已知论文 / 单论文 Evidence 定位
  -> Dense

自然问题 / Discovery
  -> MMR Dense，candidate_pool=50，lambda=0.85

Diversified Dense
  -> 保留为可解释的来源多样化 Baseline
```

当前 `ask`、`search` 和 `show-prompt` 仍使用 Dense。MMR 已作为 Evaluation Strategy 实现，
但还没有接入显式 `retrieval_mode` 或自动 Query Routing。

## 10. 当前系统状态

已经完成：

- PDF Parse、Page-aware Chunk、Metadata 和稳定 Chunk ID。
- SiliconFlow `BAAI/bge-m3` Embedding。
- Qdrant 向量存储和幂等 Corpus Ingestion。
- Dense、Diversified Dense、MMR Dense、BM25、Hybrid RRF 和 Hybrid RRF + MMR。
- Prompt、答案和基础 Citation 输出。
- 两套 Retriever Evaluation Dataset。
- Paper、Page、MRR、冗余和延迟指标。
- 可复现 Baseline JSON、Markdown Report 和 Dataset SHA-256。
- 单元测试、Qdrant 集成测试、Ruff、mypy 和依赖检查。

最近一次全量验证：

```text
50 passed
Ruff: All checks passed
mypy: Success
pip check: No broken requirements
16 个 Baseline JSON Schema 校验通过
所有 Dataset SHA-256 匹配
```

当前未完成：

- Cross-Encoder Reranker。
- Query Rewrite。
- 完整 Claim-Evidence Citation Verification。
- Answer Evaluation 和 LLM-as-a-judge。
- FastAPI 和前端。
- Planner、Research Agent、Reflection 和 LangGraph Runtime。

## 11. 下一步开发顺序

### 11.1 让多策略进入实际检索链路

先为 `search`、`ask` 和后续 FastAPI 增加显式 `retrieval_mode`：

```text
evidence -> Dense
discovery -> Hybrid RRF
```

在没有 Agent 前先由用户或 API 显式选择，避免未经评估的自动路由引入额外变量。

### 11.2 扩大种子语料并建立 Corpus v2

保留 `agent-seed-v1` 作为固定回归集，新增同主题、容易混淆的论文形成
`agent-seed-v2`。不能直接修改 v1，否则历史 Baseline 无法复现。

重点增加 Tool Use、Planning、Memory、Multi-Agent 和 Agent Benchmark 方向的相似论文，
并同步增加 Hard Negative，而不是只增加 PDF 数量。

### 11.3 评估 Query Rewrite

当前 BM25 对没有英文词面重合的中文问题会返回空结果。Corpus v2 建立后，再独立比较：

```text
原始中文 Query
  vs
英文 Lexical Query Rewrite
```

### 11.4 再决定是否引入 Reranker

只有 Hybrid + RRF 的 Baseline 明确后，再加入 Cross-Encoder Reranker。需要比较提升、延迟
和部署成本，不能默认“组件越多效果越好”。

### 11.5 完善生成与引用评测

Retriever 稳定后，再加入：

- Citation completeness。
- Citation correctness。
- Claim-Evidence entailment。
- Answer faithfulness。
- Report structure quality。

### 11.6 最后引入 Agent

Agent 阶段预计增加：

```text
Research Planner
  -> Research Tasks
  -> Query Rewrite / Multi-query Retrieval
  -> Evidence Collection
  -> Reflection / Verification
  -> Report Generation
```

Agent 的价值应体现在动态研究计划、多轮证据补全、失败恢复和引用验证，而不是简单地把固定
RAG 链路包进 LangGraph。

## 12. 当前限制和评测注意事项

- 每套 Evaluation Dataset 只有 25 条，是开发诊断集，不是统计显著的公开 Benchmark。
- Discovery 问题由项目开发阶段人工编写，可能与论文原文措辞较接近。
- Exact Page 标注不是所有相关页面的穷举。
- Same-page Redundancy 只衡量页面重复，不等同于语义层面的 Intra-list Diversity。
- ReAct 等少数 PDF 页存在字体到 Unicode 映射乱码，Parser 质量可能影响 Retriever。
- 当前没有自动 Query Router，因此不能声称系统已经会自动选择 Dense 或 Hybrid。
- 当前没有 Agent，因此不能声称系统已经具备自主规划、工具调用或长期记忆能力。

## 13. 后续优化记录模板

后续新增关键功能或优化时，在本文末尾追加以下结构：

```markdown
## YYYY-MM-DD：优化名称

### 原有设计

描述优化前的实现和 Baseline。

### 问题与证据

记录失败 Case、指标、日志或可复现现象。

### 优化目标

明确希望改善的指标，以及允许接受的退化范围。

### 方案选择

记录候选方案、最终方案和取舍理由。

### 实施措施

记录模块、接口、参数和关键工程决策。

### 实验设置

记录 Dataset、SHA-256、Corpus、Embedding、Chunking、Collection、参数和命令。

### 实验结果

同时记录提升、退化、延迟和失败 Case。

### 最终决策

说明是否替换默认方案、适用范围和下一步工作。
```

## 14. 2026-08-16：引入 BM25、RRF 和 Hybrid Retrieval

### 原有设计

此前已经完成 Dense、规则多样化和 MMR。Discovery 最优实验策略是 MMR Dense
`lambda=0.85`，但系统还没有词面检索，也无法利用 Dense 与关键词匹配的互补性。

### 问题与证据

- `DS-001` 的 Dense Top-1 召回错误论文，说明纯语义相似度存在概念歧义。
- Discovery Dense 的 Cross-paper Complete Coverage@10 只有 80%。
- 用户问题主要是中文、论文主要是英文，BM25 可能因没有词面重合而失效。
- 如果同时扩充 Corpus 和修改 Retriever，将无法判断指标变化来自数据还是算法。

### 优化目标

- 固定 `agent-seed-v1`、571 Chunks 和两套 Evaluation Dataset。
- 建立可复现的 BM25-only Baseline。
- 验证 Dense + BM25 的 RRF 是否改善 Top-1、MRR 和跨论文覆盖。
- 验证 MMR 作用在融合候选池后是否继续产生收益。
- 不允许 Hybrid 损害已知论文 Evidence 定位却仍被设置为全局默认。

### 方案选择

使用 `bm25s` 实现标准 BM25，使用 `PyStemmer` 做英文词干化和英文停用词过滤；不在第一轮
加入 LLM Query Rewrite，先真实测量中文 Query 对英文语料的词面检索边界。

融合使用 Reciprocal Rank Fusion：

```text
RRF score(d) = sum(1 / (rrf_k + rank_i(d)))
```

RRF 不要求 Dense cosine score 与 BM25 score 同尺度，适合当前异构 Retriever。

### 实施措施

```text
Qdrant 读取并按 chunk_id 排序 571 Chunks
  -> BM25 English stopwords + Porter stemming
  -> Dense Top-50
  -> BM25 Top-50（过滤 score <= 0）
  -> RRF，k=60
  -> 可选 MMR，lambda=0.85
```

- BM25 索引直接从当前 Qdrant Payload 构建，不重新 Parse PDF。
- 索引构建前按稳定 `chunk_id` 排序，保证同分结果可复现。
- RRF 按 `chunk_id` 去重并重新生成 Citation ID。
- Hybrid+MMR 对 RRF 分数先做 Min-Max 归一化，再和余弦冗余组合。
- 新增 `bm25`、`hybrid_rrf`、`hybrid_rrf_mmr` Evaluation Strategy。
- Baseline 记录 BM25、RRF、Candidate Pool、MMR 和分词参数。

BM25 启动成本：

```text
Qdrant scroll 571 Chunks: 96.88 ms
BM25 index: 502.85 ms
Total startup: 599.73 ms
```

启动成本不计入单 Query latency，在线服务应在启动时构建一次索引并复用。

### 实验设置

```text
Corpus: agent-seed-v1
Chunks: 571
Dense: BAAI/bge-m3
BM25: k1=1.5, b=0.75
Candidate Pool: 50 + 50
RRF k: 60
Hybrid MMR lambda: 0.85
Top-K: 10
```

### Discovery 实验结果

| Strategy | Paper@1 | Paper@10 | Cross Complete@10 | Exact Page@5 | Exact Page@10 | Paper MRR | Page MRR | P50 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Dense | 86% | 98% | 80% | 90% | 96% | 0.9733 | 0.7543 | 230 ms |
| BM25 | 70% | 88% | 100% | 74% | 82% | 0.8300 | 0.5853 | 0.31 ms |
| Hybrid RRF | 90% | 100% | 100% | 90% | 96% | 1.0000 | 0.8170 | 243 ms |
| Hybrid RRF + MMR | 90% | 100% | 100% | 90% | 96% | 1.0000 | 0.8247 | 258 ms |

效果：

- Hybrid 修复 `DS-001` Top-1，并消除所有 Top-10 Paper 漏召回。
- Paper MRR 从 `0.9733` 提升到 `1.0000`。
- Exact Page MRR 从 `0.7543` 提升到 `0.8170`。
- Hybrid+MMR 将 Exact Page MRR 小幅提升到 `0.8247`，但同页冗余只从 16.8% 降到
  16.0%，收益较小。
- BM25 在 `DS-002`、`DS-019`、`DS-020` 上没有正分结果，证实中英词面不重合问题；Dense
  分支使这些问题在 Hybrid 中仍能正常召回。

### 已知论文集结果

| Strategy | Paper@10 | Cross Complete@10 | Exact Page@5 | Exact Page@10 | Paper MRR | Page MRR |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| Dense | 98% | 80% | 82% | 86% | 0.9733 | 0.7670 |
| BM25 | 94% | 40% | 60% | 70% | 1.0000 | 0.4808 |
| Hybrid RRF | 96% | 60% | 76% | 84% | 0.9800 | 0.6447 |
| Hybrid RRF + MMR | 98% | 80% | 76% | 84% | 0.9800 | 0.6447 |

问题包含论文名时，BM25 容易把“出现论文标题但不是目标证据页”的 Chunk 提前，导致页码
定位下降。Hybrid 因此没有超过 Dense。

### 最终决策

```text
已知论文 / Evidence 定位
  -> Dense

自然问题 / Discovery
  -> Hybrid RRF

Hybrid RRF + MMR
  -> 保留实验策略，当前增益不足以设为默认
```

不立即加入 Query Rewrite。当前 Hybrid 已经通过 Dense 分支弥补 BM25 空召回；应先建立
更困难的 Corpus v2，再判断英文 Lexical Query Rewrite 是否具有稳定收益。

## 15. 2026-08-16：统一 Retriever Factory 接入主链路

### 原有设计

Dense、BM25 和 Hybrid RRF 已完成离线评测，但 `search`、`show-prompt` 和 `ask` 的真实主链路
仍固定依赖 Dense。Hybrid 只存在于 `evaluate-retriever` 命令，评测结论还没有转化为产品行为。

### 问题与证据

- Discovery 评测支持 Hybrid RRF，但真实问答无法使用它，存在 Evaluation/Runtime 脱节。
- CLI 中 Retriever 组装逻辑分散，后续 FastAPI 或 Agent Runtime 容易重复实现。
- BM25 构建约需 600 ms；如果每个 Query 重建，会引入不必要的固定延迟。
- 当前没有 Query Router Dataset，自动选择模式缺乏可评测依据。

### 优化目标

- 建立统一的 Retriever 创建和选择边界。
- 将评测结论显式映射到真实 Search、Prompt 和 Answer 链路。
- 在一个 Runtime 生命周期内只构建一次 BM25/Hybrid 实例。
- 暂不引入自动路由，保证行为可解释、可复现。

### 方案选择

```text
retrieval_mode=evidence  -> Dense
retrieval_mode=discovery -> Dense + BM25 + RRF
```

`evidence` 用于已知论文或明确主题内的页面证据定位；`discovery` 用于自然问题和跨论文发现。
CLI 默认 `evidence` 以兼容既有命令，但允许调用方显式指定两种模式。

### 实施措施

- 新增 `RetrievalMode` 和 `RetrieverFactory`。
- Factory 启动时持有 Dense；首次请求 `discovery` 时从 Qdrant 读取 Chunk 并构建 BM25 与
  `RrfFusionRetriever`。
- Factory 缓存 Hybrid 实例，同一 Runtime 后续 Query 不重复加载 Chunk 或构建 BM25。
- `RetrievalRuntime` 统一暴露 `retriever_for(mode)` 和 `retrieve(question, top_k, mode)`。
- `BaseRagPipeline.ask` 增加 `retrieval_mode`，Evidence 经同一 Factory 进入 `AnswerGenerator`。
- `search`、`show-prompt`、`ask` 增加 `--retrieval-mode evidence|discovery`。
- Evaluation 仍保留六种实验 Strategy，不与两个生产模式混淆。

### 验证结果

- Factory 测试确认 `evidence` 返回 Dense，`discovery` 返回 Hybrid RRF。
- 连续两次请求 `discovery` 返回同一 Hybrid 实例，Chunk Loader 只调用一次。
- CLI 测试确认 `search`、`show-prompt` 和 `ask` 将 `discovery` 传到 Runtime/Pipeline。
- Pipeline 测试为两种模式注入不同 Evidence，确认对应 Evidence 同时进入 LLM User Prompt
  和最终 Answer Citation。
- 真实 `show-prompt` Smoke Test：`evidence` 返回 ReAct 第 3 页，`discovery` 返回 Reflexion
  第 2 页，两者均以 `[C1]` 进入最终 User Prompt。
- 真实 `ask --top-k 1` Smoke Test：两种模式均完成 LLM 生成并通过 Citation Validation；
  最终 Sources 分别指向 ReAct 第 3 页和 Reflexion 第 2 页。
- 全量测试 `57 passed`；Ruff 和 mypy 均通过。

### 最终决策

当前主链路正式支持显式双模式，但不能声称系统会自动识别 Query 类型。自动 Query Router
应在建立路由标签、错误成本和独立 Evaluation 后再实现。

## 16. 2026-08-16：构建 Corpus v2 与 Retrieval Discovery v2

### 原有设计

`agent-seed-v1` 只有 10 篇论文、347 页和 571 Chunks。25 条 Discovery 问题在该语料上已被
Dense/Hybrid 较好解决，Hybrid RRF 的 Paper Recall@10 达到 100%，但同主题论文较少，无法
充分检验 Retriever 对 Reflection、Planning、Memory、Tool Use 和 Multi-Agent 方法的区分。

### 问题与证据

- v1 中每个主题通常只有一到两篇论文，Paper Recall 容易因主题词直接命中而偏高。
- 如果同时替换全部问题和扩大 Corpus，无法判断退化来自新增干扰论文还是问题难度。
- 新增论文必须固定 revision、SHA-256 和 Parse 状态，否则 Evaluation 无法复现。
- 页码标注必须基于实际 PDF 文本层核对，不能只根据摘要推测。

### 优化目标

- 将 Corpus 扩大到约 20 篇，同时保持 Agent 领域边界和小型可维护规模。
- 为主要主题加入同方向、不同机制的 Hard Negative。
- 建立 50 条无标题泄漏的 Discovery v2，并保留 v1 paired anchors。
- 在 Embedding 和 Retriever 实验之前完成 PDF、Parse、Chunk 与 Dataset 质量门禁。

### Corpus v2 方案

v2 保留 v1 的 10 篇论文，新增：

| 方向 | 新增论文 | 主要干扰对象 |
| --- | --- | --- |
| Tool Routing | MRKL | ReAct、Toolformer |
| Self Feedback | Self-Refine | Reflexion |
| Search Planning | Tree of Thoughts | LATS |
| Long-term Memory | MemoryBank | MemGPT、Generative Agents |
| Model Orchestration | HuggingGPT | ReAct、Voyager |
| API Calling | Gorilla、ToolLLM | Toolformer、MRKL |
| Multi-Agent | AgentVerse、MetaGPT | CAMEL、AutoGen |
| Evaluation | WebArena | AgentBench |

所有新增论文通过 arXiv API 核对正式标题和最新 revision，并将 revision 固定到下载 URL。
v1 PDF 原样复制到 v2，新增论文单独下载。

### Corpus v2 结果

```text
Corpus: agent-seed-v2
Papers: 20
Pages: 604
Chunks: 971
Chunking: chunking_v1
Parse: 14 verified / 6 warning / 0 failed
Non-empty pages: 100%
Visual check: passed
```

Tree of Thoughts 第 2、5、7、8 页被 Parser 标记为可疑字符页，但 Poppler 渲染显示页面完整
可读，问题来自引用和字体的文本映射。v1 已知的 ReAct、Toolformer、Reflexion、AgentBench
字符警告与 CAMEL 标题匹配警告继续保留，不隐藏 Parser 局限。

### Evaluation v2 方案

```text
DS-001..025: v1 paired anchors
DS-026..040: 15 single-paper hard negatives
DS-041..050: 10 cross-paper comparisons
Total: 50 cases
Cross-paper: 15
Hard Negative: 30
Target coverage: 20 / 20 papers
```

前 25 条的问题和 Relevant Pages 与 v1 完全一致，只增加 `anchor-v1` 标签。这样在 v2
Collection 上重跑时，可以单独测量新增 10 篇干扰论文造成的指标变化。后 25 条专门测试新增
论文和相邻机制的区分，全部标记为 Hard Negative。

### 实施与质量门禁

- Corpus Spec 固定 arXiv revision 和下载 URL。
- Manifest 记录文件大小、SHA-256、页数、文本字符数、异常页和 Parse 状态。
- Poppler 检查 20 个首页和 10 个异常页，视觉状态为 passed。
- Chunk 统计确认 971 个稳定 Chunk，无重复 ID 或空 Chunk。
- 新增离线 `search_corpus_pages.py`，按 Parser 文本搜索页码和上下文，辅助人工标注。
- Dataset Validator 现在同时检查 Schema、Case ID、论文 ID、页码范围和 Title Leakage。
- 单元测试确认 v2 有 50 条、15 条 cross-paper、30 条 Hard Negative，覆盖全部 20 篇；
  前 25 条的 Question 和 Relevant Pages 与 v1 一致。

### 最终决策

Corpus v2 和 Evaluation v2 已完成离线冻结，但尚未写入 Qdrant。下一步先建立独立的
`agent_seed_v2_bge_m3_chunking_v1` Collection，再在相同 v2 Dataset 上评估 Dense、BM25、
Hybrid RRF，不能复用或混写 v1 Collection。

## 17. 2026-08-17：Corpus v2 向量化与 Retrieval Evaluation v2

### 优化原因

v1 只有 10 篇论文，Retriever 的高分可能部分来自候选空间较小。需要把 v2 的 20 篇同主题
论文真正写入独立 Collection，并用 paired anchors 区分“语料扩大造成的退化”和“新增问题
本身的难度”。同时必须验证重复导入不会制造重复点。

### 实施措施

- 为 `ingest-corpus` 和 `evaluate-retriever` 增加显式 `--collection`，不修改 `.env`，避免
  v1/v2 混写。
- 使用 SiliconFlow `BAAI/bge-m3` 为 971 个 `chunking_v1` Chunk 生成 1024 维向量。
- 写入 `agent_seed_v2_bge_m3_chunking_v1` 后重复执行一次完整导入。
- Evaluation 报告根据 Dataset tag 自动拆分 `paired_anchor` 和 `challenge`，原始逐题结果也
  保存该字段。
- 在同一 Collection、Dataset 和 Top-K 下运行 Dense、BM25、Hybrid RRF；Hybrid 参数固定为
  Candidate Pool 50、RRF k=60。

### 导入验证

```text
第一次：20 papers / 604 pages / 971 chunks / Qdrant points=971
第二次：20 papers / 604 pages / 971 chunks / Qdrant points=971
```

第二次没有增长到 1942，证明当前按
`corpus_id + corpus_version + paper_id + chunking_version` 替换写入是幂等的。

### Evaluation v2 总体效果

| Strategy | Paper@10 | Exact Page@10 | Paper MRR | Page MRR | Redundancy@10 |
| --- | ---: | ---: | ---: | ---: | ---: |
| Dense | 95.50% | 87.83% | 0.9200 | 0.7190 | 12.80% |
| BM25 | 86.83% | 81.33% | 0.8620 | 0.6493 | 13.04% |
| Hybrid RRF | **95.83%** | **91.83%** | **0.9640** | **0.8099** | 15.20% |

### 分组结果

paired anchors 的 Paper@10：Dense `98%`、BM25 `88%`、Hybrid `98%`。与相同问题的 v1
相比，Dense 不变，BM25 不变，Hybrid 下降 2 个百分点；但 Dense Exact Page@10 从 `96%`
降到 `90%`，说明新增同主题论文主要影响排序和精确证据页定位。

challenge 的 Paper@10：Dense `93.00%`、BM25 `85.67%`、Hybrid `93.67%`；Paper MRR 分别
为 `0.8900 / 0.9080 / 0.9600`。Hybrid 总体最好，但 Complete Papers@10 为 `84%`，低于
Dense 的 `88%`，不能声称 Hybrid 对每个跨论文问题都更好。

### 失败分析与当前决策

- BM25 在 `DS-002`、`DS-019`、`DS-020`、`DS-047` 上无正分结果，暴露纯中文 Query 与
  英文 Corpus 的词面不重合。
- Hybrid 在 `DS-021`、`DS-041`、`DS-045`、`DS-046`、`DS-049` 未完整覆盖全部目标论文。
  三篇或四篇目标论文的 Case 容易被已经命中的论文占满 Top-10。
- Hybrid 提高了长列表覆盖与 MRR，但 Exact Page@5 低于 Dense，且同页冗余更高。

因此本轮只记录 Query Rewrite 和 Reranker 的潜在作用，不立即加入。下一步先按“跨语言词面
缺口”“同主题机制区分”“多目标候选挤占”拆分失败 Case，再用独立消融决定应该增加哪种
复杂度。完整表格见 `evals/baselines/retrieval_discovery_v2_comparison.md`。

## 18. 2026-08-17：Hybrid RRF + MMR v2 消融

### 优化原因

原始 Hybrid RRF 在 challenge 上的 Complete Papers@10 为 `84%`，低于 Dense 的 `88%`，
且 Same-page Redundancy@10 为 `16.80%`。需要验证失败是否主要来自候选重复，以及 MMR 能否
在不损伤证据定位的前提下改善多论文覆盖。

### 实验设置

```text
Corpus / Dataset / Top-K: 与 Retrieval Discovery v2 Baseline 相同
Dense candidate pool: 50
BM25 candidate pool: 50
RRF k: 60
MMR lambda: 0.70 / 0.85 / 0.95
```

验收条件是 Challenge Complete Papers@10 至少从 `84%` 提升到 `88%`，Exact Page@10 下降
不超过 2 个百分点，并至少修复两个已知失败 Case。

### 实验结果

| Strategy | Paper@10 | Exact Page@10 | Redundancy@10 | Challenge Complete@10 | Challenge Redundancy@10 |
| --- | ---: | ---: | ---: | ---: | ---: |
| Hybrid RRF | 95.83% | **91.83%** | 15.20% | 84% | 16.80% |
| + MMR 0.70 | 95.83% | 89.83% | **14.40%** | 84% | **16.00%** |
| + MMR 0.85 | 95.83% | **91.83%** | 15.00% | 84% | 16.40% |
| + MMR 0.95 | 95.83% | **91.83%** | 15.60% | 84% | 16.80% |

三组都没有修复 `DS-021`、`DS-041`、`DS-045`、`DS-046`、`DS-049` 中任何一个缺失目标
论文。`0.70` 虽降低少量冗余并提高总体 Exact Page@5，但 paired Exact Page@10 从 `96%`
降到 `92%`；`0.85` 与原始 Hybrid 几乎相同；`0.95` 反而增加冗余。MMR 的 P50 也比原始
Hybrid 增加约 90--140 ms。

### 最终决策

本实验未达到验收条件，MMR 不加入默认 Discovery 链路，只保留为可复现的消融 Strategy。
这些失败不只是“结果太相似”，更可能是中文 Query 没有充分表达英文检索词、跨论文问题
没有拆成多个子意图，或目标候选进入 Pool 后排名仍过低。下一步优先做 Query Rewrite 的
离线可观察实现与对照评测，再根据候选召回情况决定是否需要 Reranker。

## 19. 2026-08-17：Query Rewrite v2 对照实验

### 优化原因

BM25 对 4 条中文问题没有正分，多论文问题还可能把多个机制压缩在一个 Query 中。MMR 未能
修复任何覆盖失败，说明需要先改善 Query 与候选生成，而不是只重排已有结果。

### 实施方案

```text
Original Chinese Query
  + DeepSeek 生成 3 条互补 English Query
  -> 每条 Query 分别执行 Dense Top-50 + BM25 Top-50
  -> 8 路 Ranking 使用 RRF k=60 融合
  -> Top-10
```

- Prompt 禁止回答问题、猜测论文名、系统名、作者、Dataset 或 arXiv ID。
- Rewrite 逐题缓存，记录模型、`query_rewrite_v1`、精确 Query 和生成耗时。
- Evaluation Raw Result 新增 `retrieval_queries`，可以检查每个结果由哪些 Query 产生。
- LLM Rewrite 在检索计时前预生成，避免把生成延迟混入纯 Retrieval latency，但单独报告。
- HTTP Transport 对网络错误、429、5xx 做最多 3 次指数退避重试；其他错误不重试。

### 质量门禁

```text
Cases: 50
Rewrites: 150
Corpus title / slug leakage hits: 0
Rewrite model: deepseek-v4-flash
Mean rewrite generation latency: 6615.84 ms
```

### 实验结果

| Strategy | Paper@10 | Exact Page@5 | Exact Page@10 | Paper MRR | Page MRR | Redundancy@10 | Retrieval P50 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Hybrid RRF | 95.83% | 80.83% | 91.83% | 0.9640 | 0.8099 | **15.20%** | **265 ms** |
| + Query Rewrite | **97.00%** | **90.17%** | **92.33%** | **0.9767** | **0.8340** | 16.20% | 1451 ms |

Challenge Complete Papers@10 从 `84%` 提升到 `96%`，修复 `DS-045`、`DS-046`、`DS-049`；
paired Complete Papers@10 从 `96%` 降到 `92%`，新增 `DS-025` 回退。`DS-021` 和 `DS-041`
仍未修复。

`DS-025` 的 Rewrite 多次强调 open-world、continuous learning 和 skill library，使同一目标
论文占满 Top-10，把另一篇 Tree Search 论文挤出。这说明等权多 Query RRF 会放大重复意图，
不能简单增加 Rewrite 数量。

### 最终决策

Query Rewrite 对困难、多子意图问题有明确价值，证明跨语言和 Query 表达是主要瓶颈之一。
但它带来约 `6.6 s` 生成成本、约 5.5 倍纯检索 P50、paired 回退及额外模型依赖，本轮不替换
默认 Discovery。下一步检查融合 Candidate Pool 的目标召回位置，再决定 Reranker 是否能修复
`DS-021/025/041`，或先为重复子意图加入 Query 级权重与去重。

## 20. 2026-08-17：Query Rewrite Candidate Pool 诊断

### 诊断目标

判断 `DS-021/025/041` 属于 Candidate Recall Failure，还是目标已经进入 Top-50、仅因融合
排序未进入 Top-10。只有后者才适合 Reranker。

### 实施措施

- 抽取通用 `fuse_rankings`，确保生产 Hybrid、Multi-query Hybrid 和诊断工具使用完全相同的
  RRF 公式与确定性 Tie-break。
- 新增 `scripts/diagnose_query_rewrite_candidates.py`，复用固定 Rewrite 缓存，不调用 LLM。
- 对原始 Query 与 3 条 Rewrite 分别记录 Dense/BM25 Top-50。
- 对每个目标记录 Original RRF Rank、Rewrite RRF Rank、Exact Page Rank 和各分支最佳排名。
- 生成版本化 JSON 与 Markdown，详细分支结果保留在 JSON 中。

### 诊断结果

| Case | 缺失目标 | Original Rank | Rewrite Rank | Exact Page Rank | Classification |
| --- | --- | ---: | ---: | ---: | --- |
| DS-021 | ReAct | 17 | 31 | 39 | rerankable_11_50 |
| DS-025 | LATS | 4 | 14 | 14 | rerankable_11_50 |
| DS-041 | Self-Refine | 15 | 18 | 18 | rerankable_11_50 |

三个缺失目标的严格页码都进入融合 Top-50，所以不是召回失败。`DS-021` 的 ReAct 在专门
Rewrite 上达到 Dense 第 3、BM25 第 1，却因 Toolformer 在其他 Query 中反复高排，最终融合
降到第 31。`DS-025` 的 LATS 在 Original RRF 中本来是第 4，加入 Rewrite 后反而降到第 14。

### 当前决策

Reranker 具有候选基础，但当前根因更具体：等权 Multi-query RRF 通过分数累加奖励重复命中，
没有保证不同子意图被覆盖。由于 Query Rewrite 已带来较高延迟，下一步先做无需额外模型调用
的 Query-aware Fusion 消融，例如 `max-over-query RRF` 或 intent-balanced interleaving；如果
仍不能稳定修复三个 Case，再加入 Cross-Encoder Reranker。这样可以分别量化 Fusion 与
Reranker 的真实贡献，而不是把两种变化同时引入。

## 21. 2026-08-17：Query-aware Fusion 消融

### 实验设计

为了排除外部 API 波动，50 个 Case 的 Original Query 与 3 条固定 Rewrite 只执行一次
Dense/BM25 Top-50。六种策略共享完全相同的分支 Ranking，只离线替换 Fusion：No Rewrite、
Flat Sum、Original 权重 `0.4/0.5/0.6`、Max-over-query。

预设验收要求：至少修复 `DS-021/025/041` 中 2 条，paired/challenge Complete Papers@10
分别不低于 `96%/96%`，Exact Page@10 不低于 `92.33%`，并保持 `DS-045/046/049`。

### 实验结果

| Strategy | Paper@10 | Exact Page@10 | Page MRR | Paired Complete | Challenge Complete | Focus Fixed | Pass |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| No Rewrite | 95.83% | 91.83% | 0.8099 | 96% | 84% | 1/3 | False |
| Flat Sum | 97.00% | 92.33% | **0.8340** | 92% | 96% | 0/3 | False |
| Weighted original=0.4 | 98.00% | 93.33% | 0.8190 | 96% | 96% | 1/3 | False |
| Weighted original=0.5 | 96.83% | 91.83% | 0.8273 | 96% | 88% | 1/3 | False |
| Weighted original=0.6 | 95.83% | 90.83% | 0.8130 | 96% | 84% | 1/3 | False |
| Max-over-query | **99.00%** | **97.00%** | 0.8055 | **100%** | **96%** | **2/3** | **True** |

### 原因分析

Max-over-query 不再累计同一 Chunk 在多条相似 Query 中的重复得分，因此 ReAct 在 `DS-021`
进入第 4，LATS 在 `DS-025` 从 Flat Sum 的 Top-10 外提升到第 1；`DS-045/046/049` 仍完整
覆盖。唯一剩余失败为 `DS-041` 的 Self-Refine。

代价是 Exact Page@1 从 Flat Sum 的 `60.17%` 降到 `54.67%`，Page MRR 从 `0.8340` 降到
`0.8055`，虽然 Exact Page@10 提升到 `97%`。Max-over-query 更擅长覆盖多个子意图和论文，
但不保证严格标注页最先出现。

### 最终决策

Max-over-query 作为当前最佳 Query Rewrite Fusion 实验策略，融合平均耗时约 `0.46 ms`，不
增加模型调用。但 Query Rewrite 本身仍有约 6.6 秒生成延迟，且 Page MRR 有回退，因此不
替换默认低延迟 Hybrid RRF。下一步针对已经位于 Top-50 的唯一失败 `DS-041` 引入 Reranker
对照，并将 Page MRR、覆盖提升和端到端延迟同时纳入验收。

## 22. 2026-08-17：Cross-Encoder Reranker 实现与消融

### 优化原因

Query-aware Fusion 后唯一未完整覆盖的 `DS-041` 并非候选召回失败：Self-Refine 论文和严格
标注页都在 Top-50，只是分别位于第 24。因此需要验证 Cross-Encoder 能否利用 Query-Document
联合编码，将语义相关但初始排名靠后的候选提升到 Top-10。同时不能只测试最佳 Deep 链路，
还要测量 Reranker 对低延迟 Fast Hybrid 的独立贡献。

### 实现措施

- 新增 `SiliconFlowRerankerProvider`，模型为 `BAAI/bge-reranker-v2-m3`，调用 `/rerank`。
- 新增可组合的 `RerankingRetriever`：Candidate Retriever 先返回 Top-50，Reranker 再选
  Top-10，不侵入 Dense、BM25、RRF 或 Query Rewrite 的内部实现。
- Trace 记录每个 Chunk 的 original rank/score、reranker score、reranked rank，以及模型名、
  候选数和独立调用延迟。
- Provider 对空 Query/Document、结果数量、index 越界、重复 index 和非法 score 做校验；
  网络错误、429、5xx 复用共享 Transport 的最多 3 次指数退避。
- 真实 API Smoke Test 验证相关文本得分 `0.249847`，无关文本得分 `0.000049`。
- 完整实验固定 Corpus v2、50 Case、Top-50 Candidate Pool 和 Top-10 输出，只改变是否执行
  Query Rewrite/Max-over-query 与 Reranker，避免混淆各优化的贡献。

四组链路如下：

```text
Fast Base: Original Query -> Dense/BM25 -> RRF Top-50 -> Top-10
Fast Rerank: Fast Base Top-50 -> BGE Reranker -> Top-10
Deep Base: Original + 3 Rewrites -> Dense/BM25 -> Max-over-query Top-50 -> Top-10
Deep Rerank: Deep Base Top-50 -> BGE Reranker(original Chinese Query) -> Top-10
```

### 实验结果

| Strategy | Paper@10 | Page@10 | Paper MRR | Page MRR | Paired Complete | Challenge Complete | Retrieval P50 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Fast Base | 95.83% | 91.83% | 0.9640 | 0.8099 | 96% | 84% | 349 ms |
| Fast + Reranker | **98.50%** | **98.00%** | **0.9900** | **0.8412** | 96% | **96%** | 1748 ms |
| Deep Max Base | **99.00%** | 97.00% | **1.0000** | 0.8055 | **100%** | 96% | 1416 ms |
| Deep Max + Reranker | 98.50% | **98.00%** | 0.9867 | **0.8492** | 96% | 96% | 2781 ms |

Reranker 自身调用 P50/P95：Fast `1390/1579 ms`，Deep `1361/1726 ms`。Deep 表中的 Retrieval
延迟只包含固定 Rewrite 的四条 Query 检索与重排，不包含平均约 `6.6 s` 的在线 Rewrite
生成时间。

### 效果与失败分析

Fast + Reranker 的收益明确：challenge 完整覆盖提升 12 个百分点，Page@10 提升 6.17 个
百分点，Page MRR 也从 `0.8099` 提升到 `0.8412`。它证明 Reranker 不只是修复单个 Case，
对跨语言、近邻方法和长尾候选排序具有系统性价值。

Deep + Reranker 修复了目标 `DS-041`：Self-Refine 从 Paper/Exact Page 第 `24/24` 提升到
`5/5`。但它同时产生两个关键回退：`DS-021` 的 ReAct 从 Paper 第 4 降到第 16，`DS-046`
的一个目标从第 2 降到第 14；paired Complete 从 `100%` 降到 `96%`。因此没有通过“修复
DS-041、保持 DS-021/025/045/046/049、Paper@10 不低于 99%”的预设验收。

根因不是模型总体相关性差，而是目标函数不同。Cross-Encoder 独立判断每个 Chunk 与整条
Query 的相关性；cross-paper 问题要求的是多个子意图、多个目标论文的集合覆盖。单独按
relevance score 排序可能让一个高相关论文占据更多位置，并挤掉另一个必要目标。

### 最终决策

Fast Hybrid RRF + Reranker 保留为可选高质量增强策略候选；Deep Query Rewrite +
Max-over-query + Reranker 保留为可复现消融，不进入默认链路。默认 Discovery 继续使用
低延迟 Hybrid RRF，避免把约 `1.4 s` 外部调用隐式加到所有请求。

这一结果比“所有模块叠加就是最佳方案”更有价值：Evaluation 明确证明不同优化之间存在
interaction effect。若后续产品链路需要高质量模式，下一步应设计 `fast/deep` 显式质量档位，
并针对 cross-paper 问题验证 coverage-aware reranking 或按子意图分配结果配额；当前不继续
无依据调参。

## 23. 2026-08-17：Answer + Citation Evaluation v1

### 开发原因

Retrieval 指标只能说明目标论文或页码是否进入 Top-K，不能证明最终答案更正确。尤其
Reranker 在 Retrieval v2 上只有局部提升，还增加约 1.4 秒延迟，因此需要验证这部分增益是否
真正转化为答案关键点、引用覆盖和拒答能力，而不是继续优化离线 Recall。

### 数据集与接口

建立 `answer_generation_v1.jsonl`，共 20 条：18 条可回答问题（15 条单论文、3 条跨论文）和
2 条 Corpus 外拒答问题。每条可回答 Case 包含 reference answer、目标论文/页码与人工 Key
Point aliases；拒答 Case 不伪造 gold evidence。

答案契约增加 `status=answered|insufficient_evidence`。当证据不足时，模型必须只返回
`INSUFFICIENT_EVIDENCE`，AnswerGenerator 将其转换为无 Citation 的结构化拒答。普通回答仍
必须至少有一个 Citation，且不能包含未知 Citation ID。Citation 模型补充 `paper_id`，用于
论文级引用评测。

实现 `paper-rag evaluate-answer`，支持：

```text
hybrid_rrf
hybrid_rrf_rerank
```

两种策略使用相同 Corpus、Dataset、Top-K、Answer model 和 Prompt，只改变 Retrieval。
Raw Result 保存实际 Answer、Top-K Evidence、Citations、错误、匹配 Key Points，以及
Retrieval/Generation/Total latency。

### 确定性指标

- Generation Success：Provider 输出是否通过 Answer/Citation 契约。
- Answerability Accuracy：可回答问题是否回答、语料外问题是否拒答。
- Key Point Recall：人工 aliases 的词面覆盖，不宣称语义正确。
- Citation Paper Precision/Recall：引用目标论文的准确率和覆盖率。
- Strict Page Precision/Recall：引用人工标注页的严格下界。
- Sentence Citation Coverage：句子是否有 marker，不宣称 Claim-Evidence entailment。

实现过程中发现模型常把 Citation 放在句号之后，最初的句子切分会把 marker 误算成独立句子。
评测器先将“标点 + 可选空格 + Citation”归一化为“Citation + 标点”再切句，并增加单元测试。
同时根据问题实际要求校准了三条 Key Point：删除无需复述的背景信息，避免为提高分数添加
答案中才出现的新标准。

DeepSeek 偶发返回 HTTP 成功但 content 为空。AnswerGenerator 增加仅针对 empty answer 的一次
业务层重试；网络错误、429 和 5xx 仍由 Transport 重试，其他校验错误立即失败。连续两次空
content 仍作为 Generation Failure 记录，不能静默丢弃。

### 真实实验结果

| Strategy | Gen Success | Answerability | Key Point | Citation Paper P/R | Strict Page P/R | Retrieval P50 | Total P50 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Hybrid RRF | 90% | 85% | 80.56% | 83.33% / 83.33% | 66.67% / 83.33% | 410 ms | 4319 ms |
| Hybrid + Reranker | **95%** | **95%** | **93.06%** | **94.44% / 94.44%** | **70.83% / 94.44%** | 1731 ms | 5679 ms |

两组 Dataset SHA-256 完全一致。Reranker 补入 `AE-017` 的 Self-Refine 和 `AE-018` 的
Voyager 直接证据，使两条跨论文题从失败/拒答变为 Citation Paper 与 Strict Page Recall 均
为 100% 的回答。Retrieval P50 增加约 1321 ms，端到端 P50 增加约 1360 ms，说明额外成本
基本来自 Reranker。

`AE-016` 在两组中都没有 ReAct 直接证据，且 DeepSeek 连续两次返回空 content，仍然失败。
这说明下一个 Retrieval 问题是 multi-target coverage，不应再靠普通 pointwise Reranker 或
Prompt 猜测补全。

### 当前决策

Reranker 在 Answer 层的价值比单看 Retrieval 指标更明显，可以保留为高质量模式候选；但
20 Case、单次生成尚不足以替换默认低延迟 Hybrid。下一步实现独立 LLM Judge，评价
Correctness、Claim-Evidence Faithfulness 与 Citation Completeness，并与确定性指标、原始
错误和延迟并列报告。之后再考虑 coverage-aware retrieval，而不是立刻加入 LangGraph。

## 24. 2026-08-17：gpt-5.5 LLM Judge v1

### 开发原因

Answer Evaluation v1 的 Key Point Recall 只做词面别名匹配，Sentence Citation Coverage 只
检查 marker，无法判断答案在语义上是否正确、Citation 是否真正支撑 Claim。若只展示这些
指标，强模型可能写出流畅且术语齐全、但证据并不支持的答案。

### 优化措施

通过 `RELAY_BASE_URL` 调用 `gpt-5.5`，建立独立三维 0-4 Rubric：Correctness、
Claim-Evidence Faithfulness、Citation Completeness。Judge 只接收候选答案实际引用的
Evidence，不允许用未引用 Top-K Chunk 给 Faithfulness 加分；最多审查 8 个 material claims。

没有假定 Relay 支持原生 Structured Outputs，而是使用 JSON-only Prompt、Pydantic Schema
校验和最多 3 次有限重试。每条成功调用保存 input SHA-256、model、prompt version、raw
response、parsed decision、attempts、Token usage 和 latency；逐 Case JSONL 缓存支持中断续跑，
答案、证据或 Prompt 变化会使哈希失效。

Generation Failure 与可回答题错误拒答确定性记 `0/0/0`，语料外正确拒答记 `4/4/4`，Judge
最终失败也记 0 并进入失败列表。所有 Case 都进入 Overall 分母，避免只统计成功调用样本。
Judge 分数与原 Answer、Evidence、Citation、确定性指标、错误和端到端延迟并列保存，不覆盖
任何原始信息。

### 真实结果

| Strategy | Correctness | Faithfulness | Citation Completeness | Overall | Judge P50/P95 |
| --- | ---: | ---: | ---: | ---: | ---: |
| Hybrid RRF | 85.00% | 83.75% | 85.00% | 84.58% | 20553 / 24617 ms |
| Hybrid + Reranker | **95.00%** | **93.75%** | **95.00%** | **94.58%** | 21002 / 27195 ms |

Hybrid 调用 15 条 LLM Judge、5 条确定性计分；Reranker 调用 17 条、3 条确定性计分。两组
真实调用均一次成功，返回模型均为 `gpt-5.5`，没有 Judge Failure。Reranker 修复的
`AE-017/018` 均获得 `4/4/4`，`AE-016` 保持 0 分。两组各有一题 Correctness=4 但
Faithfulness=3，证明 Judge 能识别“总体答案正确但局部证据表述不精确”。

### 当前决策与限制

LLM Judge 证明 Reranker 的 Answer 层收益具有语义和证据支撑，不只是词面指标提升；但
Judge P50 约 21 秒，因此它只用于离线 Evaluation，不进入在线 Answer 链路。当前仍有单
Judge 偏差、20 Case 样本较小、单次 Answer 生成、自一致性未验证等限制。下一步优先做低分
Case 人工审计与 Answer 多次生成稳定性实验，再决定是否实现 coverage-aware retrieval；仍不
急于加入 LangGraph。

## 25. 2026-08-17：Answer Stability v1 与低分 Case 审计

### 开发原因

LLM Judge v1 证明 Reranker 的单次 Answer 得分更高，但一次生成可能受模型措辞、空 content、
Citation 选择和输出长度影响。直接比较两次单次输出，无法区分稳定的 Retrieval 收益与偶然的
Generation 波动，因此需要固定 Evidence 后重复生成。

### 实验设计

选择 `AE-001/007/014/016/017/018/019/020` 八条代表性 Case，覆盖普通方法题、困难单论文题、
三个跨论文题和两个 Corpus 外拒答题。每个策略复用原 Answer Baseline 保存的 Top-10 Evidence，
不重新检索；每题调用 `deepseek-v4-flash` 生成 3 次，再用相同 `gpt-5.5` Rubric 逐次评分。

新增 `scripts/run_answer_stability.py` 和稳定性数据模型。Answer 与 Judge 都按
`variant/case/repetition` 缓存，输入变化会使 Judge 哈希失效；实验中断不会丢失已完成样本。
核心指标包括 Strict Run、Fully Stable Case、Status/Citation Consistency、每 Case 三次均值和
最低分。Strict Run 要求生成成功、Answerability 正确且 Judge 三维均不低于 3/4。

### 真实结果

共运行 48 个 Answer 样本：2 个策略 × 8 Case × 3 次。

| Strategy | Gen Success | Answerability | Correctness | Faithfulness | Strict Runs | Stable Cases |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| Hybrid RRF | 87.50% | 70.83% | 68.75% | 66.67% | 66.67% | 62.50% |
| Hybrid + Reranker | 87.50% | **87.50%** | **86.46%** | **83.33%** | **87.50%** | **75.00%** |

重复实验仍支持 Reranker 的收益，但揭示它不是完全稳定：

- `AE-017`：Hybrid 三次全部失败或拒答；Reranker 两次 `4/4/4`，一次 empty content。
- `AE-018`：Hybrid 三次全部拒答；Reranker 三次都通过，最低 F/CC 为 3/4。
- `AE-016`：两组 Top-10 都没有 ReAct 论文。Hybrid 为一次严格通过、一次低分、一次 empty；
  Reranker 为一次严格通过、一次无 Citation、一次 empty。偶尔回答正确依赖模型已有知识，
  不能算 Retrieval 成功。
- `AE-019/020`：两组共 12 次都正确返回 `INSUFFICIENT_EVIDENCE`。
- `AE-001`：固定 Evidence 下仍出现 3/4 与 4/4 波动，且题面“生成 Observation”存在语义
  歧义，Dataset v2 应明确 Observation 来自环境。

### 新发现与决策

本轮将原先笼统的“跨论文不稳定”拆成了三个可行动问题：Evidence coverage 不足、Answer
Provider 空 content、长回答可能在 `max_tokens=1200` 附近截断。下一步先在 Chat Completion
metadata 中记录 `finish_reason`，对截断输出拒绝或有限重试；然后只针对 `AE-016` 做
sub-query decomposition + coverage-aware merge。完成这两项后冻结 RAG/Evaluation v1，开始
最小 Agent Runtime，而不是继续扩展通用 Retriever 组件。

## 26. 2026-08-17：Generation Observability 与 Coverage-aware Retrieval v1

### 开发原因

Answer Stability v1 留下两个无法靠总体分数解释的问题。第一，DeepSeek 偶发返回空 content，
但长回答也可能只是达到输出 Token 上限；如果不读取 `finish_reason`，两种故障会被混为一类。
第二，`AE-016` 的 Hybrid 与 Global Reranker Top-10 都缺 ReAct，说明 pointwise relevance
不能保证跨论文问题的多目标覆盖。强模型偶尔仍能答对，反而会掩盖 Retrieval 缺陷。

本轮先解决生成可靠性，再做检索覆盖，仍不加入 LangGraph 或 Agent Runtime，避免同时改变
过多变量。

### Generation Observability 优化措施

- `ChatCompletion` 增加 `finish_reason`、response model 与 Token usage；允许 metadata 层保留
  empty content，让上层进行准确分类。
- AnswerGenerator 为每个 attempt 记录 outcome、content length、latency、model、usage 和
  error；outcome 区分 accepted、empty、truncated、provider error 与 invalid citation。
- 判断顺序改为先处理 `finish_reason=length`，再处理 empty。真实 Relay 返回过
  `length + empty content`，如果反过来判断就会误报为空响应。
- 截断时只允许一次压缩重试：最多 500 个中文字符或 300 个英文词，只保留问题核心和必要
  Citation。连续两次截断抛出 `TruncatedAnswerError`。
- `ANSWER_MAX_TOKENS` 从 1200 提升为 2400，并增加 Empty、Truncation、Retry Trigger、Retry
  Recovery 与 Finish Reason 分布指标。

针对 `AE-016/017/018` 的 18 个样本结果：

| Strategy | Gen Success | Answerability | Strict Runs | Truncated | Retry Recovery |
| --- | ---: | ---: | ---: | ---: | ---: |
| Hybrid RRF | 88.89% | 22.22% | 0.00% | 22.22% | 50.00% |
| Hybrid + Reranker | **100%** | **100%** | **100%** | 11.11% | **100%** |

结果证明有限重试能恢复截断，但生成成功不代表 Evidence 完整：`AE-016` 仍缺 ReAct，因此
继续进入 Coverage 实验。

### Coverage-aware 设计与防泄漏

Query Decomposer 使用普通任务模型 `deepseek-v4-flash`，将跨论文比较题拆成两个独立描述式
子查询。Prompt 明确禁止新增原问题未出现的论文名、方法名、作者、数据集或 arXiv ID。
正式运行前，使用 Corpus v2 全部 slug、完整标题和标题简称扫描输出；三题均为 0 泄漏。

检索流程为：

```text
Question
  -> Query Decomposition（2 路）
  -> 每路 Hybrid RRF Top-30
  -> 可选：每路内部 BGE Reranker
  -> Round-robin Coverage Merge（5/5）
  -> 去重并重新分配 C1-C10
```

没有在合并后做 Global Rerank，因为那会重新允许一个论文占满 Top-K。per-subquery Reranker
使用各自子查询评分，不使用完整 comparison query。Trace 记录每路候选数、最终配额、
Decomposition、Hybrid、Reranker 和总耗时；Decomposition、Answer 与 Judge 都使用 JSONL
缓存支持断点续跑。

### 四组消融结果

固定 `AE-016/017/018`、Top-10 和各策略 Evidence；每题生成 3 次并使用同一 `gpt-5.5`
Judge，共 36 个样本，其中两组 Baseline 的 18 个样本复用 Generation Observability v2，
Coverage 两组新增 18 个 Answer/Judge。

| Strategy | Complete Papers@10 | Correctness | Faithfulness | Citation Completeness | Strict Runs |
| --- | ---: | ---: | ---: | ---: | ---: |
| Hybrid RRF | 0.00% | 16.67% | 11.11% | 11.11% | 0.00% |
| Hybrid + Global Reranker | 66.67% | 97.22% | 83.33% | 94.44% | 100% |
| Decomposition + Hybrid + Coverage | **100%** | **100%** | **91.67%** | **94.44%** | **100%** |
| Coverage + per-subquery Reranker | **100%** | **100%** | 88.89% | 91.67% | **100%** |

两组 Coverage 的 Paper Recall@10 与 Exact Page Recall@10 也均为 100%，三题配额均为 `5/5`。
其中不带 Reranker 的检索约 `455-481 ms`；带 Reranker 为 `1991-2034 ms`。三条首次
Decomposition 分别约 `3.1/6.8/4.8 s`，缓存命中接近 0 ms。

### 效果解释与最终决策

Coverage 修复了 `AE-016` 的 ReAct 缺失，并保持 `AE-017/018` 不回退。Global Reranker 在
缺 ReAct Evidence 时仍取得很高 Answer 分，说明强模型会利用参数知识补全答案；这正是必须
同时报告 Retrieval coverage、确定性 Answer 指标和 Semantic Judge 的原因。

per-subquery Reranker 没有提高覆盖、Strict Run 或 Judge 均值，Faithfulness 和 Citation
Completeness 反而略低，并增加约 1.5 秒检索耗时。因此后续 cross-paper 候选采用
`Query Decomposition + Hybrid RRF + Coverage Merge`，不叠加 Reranker。

该结论目前只有 3 条定向 Case，不直接接入生产 Factory。第三步冻结 RAG/Evaluation v1 并
进入 Agent Runtime 暂缓；在此之前若要生产化 Coverage，必须先扩展 cross-paper Evaluation
并为自动 Query Router 建立单独的路由评测。

## 27. 2026-08-17：LangGraph Agent Runtime v1

### 开发原因

前 26 个阶段已经把固定 RAG、Coverage Retrieval、Answer/Citation 与三层 Evaluation 跑通。
继续增加 Retriever 组件的边际收益有限，下一步需要证明系统能够根据问题和 Evidence 状态
选择已经验证过的能力，而不是由开发者在命令行预先指定策略。

LangGraph 在这里承担 Runtime 职责：State、条件分支、有限循环和 Trace。没有用 LangChain
重写 Retriever 或 AnswerGenerator，避免核心 RAG 退化为框架黑盒。

### State 与结构化 Planner

新增 `ResearchTask`、`ResearchPlan`、`EvidenceAssessment`、`AgentEvent`、`AgentResult` 和
`ResearchState`。Plan 强制以下契约：

- `single_paper` 恰好 1 个任务。
- `cross_paper` 包含 2-4 个互不重复的任务。
- Task ID 必须从 `T1` 连续排列。
- 跨论文 Task 不能原样重复完整问题。
- 修订次数被限制为最多 1 次。

Planner 使用 Relay `gpt-5.5`，JSON-only Prompt 配合 Pydantic 校验和最多 3 次结构重试。
每次成功结果保存 input SHA-256、模型、Prompt version、生成耗时、raw response 和结构化 Plan。
Corpus v2 的 slug、完整标题和标题简称用于 Query 泄漏检查；原问题未出现的论文名不能被
Planner 偷带进 Retrieval Query。无效修订必须在写缓存前拒绝。

### LangGraph 节点与停止规则

```text
plan_research
  -> retrieve_evidence
  -> assess_evidence
       sufficient -> write_report
       insufficient -> revise_queries -> retrieve_evidence（最多一次）
  -> validate_citations
  -> END
```

Question Analyzer 合并进 `plan_research`，避免为了形式增加一次 LLM 调用。`single_paper` 使用
一次 Hybrid RRF；`cross_paper` 为每个 Task 独立执行 Hybrid Top-30，再通过 Round-robin
Coverage Merge 组织 Top-10。

Reflection 使用确定性 Evidence Gate：每个 Task 必须达到最小 Chunk 配额，跨论文问题还要
至少覆盖两个不同论文来源。第一次不足时把 Assessment 反馈给 Planner 修订 Query；第二次
仍不足则直接返回结构化 `INSUFFICIENT_EVIDENCE`，不调用 Answer 模型，防止强模型参数知识
掩盖 Retrieval 失败。

每个节点记录 sequence、outcome、latency 和关键 details。最终 Citation 节点再次验证每个
Citation ID 与 final Evidence 的 Chunk ID 一致。

### CLI 与真实 Smoke

新增：

```powershell
paper-rag agent "<question>" --show-trace
```

单论文 Smoke 使用 virtual context management 问题：

- Planner 正确输出 `single_paper / 1 task / revision=0`。
- Hybrid 检索 Top-30、选择 Top-10，没有触发 Revision。
- Planning `10678 ms`、Retrieval `314 ms`、Answer `3803 ms`。
- 最终引用 5 个 MemGPT Evidence，Citation Validation 通过。

跨论文 Smoke 使用匿名 ReAct/Toolformer 比较问题：

- Planner 正确输出 `cross_paper / 2 tasks / revision=0`，Query 无目标论文名泄漏。
- Coverage Merge 为两路分配 `T1=5 / T2=5`，同时召回 ReAct 与 Toolformer。
- Planning `10721 ms`、Retrieval `515 ms`、Answer `17646 ms`。
- 最终引用两个目标论文的 4 个 Evidence，Citation Validation 通过。

### 测试与当前决策

Planner/Runtime/CLI 测试覆盖结构校验、JSON fence、缓存、重试、泄漏、修订、单论文路由、
跨论文配额、Revision 成功、Retry 耗尽拒答、资源关闭和 CLI Trace。

当前不加入 Checkpointer、长期 Memory、Web Search、Interrupt 或多 Agent。`gpt-5.5` Planning
冷启动约 10.7 秒，是明确的产品延迟成本，不能用缓存命中数字掩盖。下一步建立 Agent-vs-
Fixed-RAG Evaluation，重点测量 Router Accuracy、Task Coverage、Revision Precision、
Complete Papers@10、Strict Run、总延迟和模型调用成本，再决定 Planner 是否需要换成更快模型。

## 28. 2026-08-18：Agent Runtime Evaluation v1

### 开发原因与对照设计

LangGraph Runtime 的两条 Smoke 只能证明流程可运行，不能证明自动 Planning 比固定 RAG 更好。
本轮固定 8 条 Answer Stability 代表题，建立四组对照：

| Variant | 定义 |
| --- | --- |
| Fixed Hybrid RRF | 低延迟固定 Baseline |
| Fixed Hybrid + Global Reranker | 质量较高的固定 Baseline |
| Oracle Route | 人工知道问题类型和 answerability 的路由上界 |
| LangGraph Agent | 自动 Planning、Routing、Coverage 和一次有限 Revision |

前两组和 Oracle 复用历史冻结 Answer/Judge 样本，避免重新采样造成对比漂移；Agent 新运行一次，
并使用相同的确定性 Answer/Citation 指标和 `gpt-5.5` Judge。Oracle 对可回答跨论文比较题使用
Coverage Hybrid，对单论文或已知语料外题使用 Hybrid，因此它是带人工先验的上界，不是可部署
系统。

新增 `agent_runtime_v1.jsonl`，每条记录 expected question type、task count、retrieval strategy、
task facets、目标论文/页码、should_retry 和 answerability。最初把两条语料外题标成
`single_paper`；审计真实 Plan 后发现问题明确要求在“当前语料/这些论文”中检索，应属于
`cross_paper`。金标改为跨论文，并分别检查正文/实验表格以及 airline/retail 两个 Facet。
这次标签修订发生在查看 Answer 质量之前，依据的是路由语义和结构化 Plan，而不是为了提高最终
分数。

### 指标与可观察性补充

除 Retrieval、Answer 和 Judge 指标外，新增：

- Router Accuracy、Task Count Accuracy、Task Facet Coverage。
- Corpus title/slug Leakage、Retrieval Strategy Accuracy。
- Unnecessary Revision、Revision Recovery 和 Citation Validation。
- Planning/Retrieval/Revision/Answer 分阶段延迟与模型调用次数。
- Answer/Judge Token usage；历史 Planner 缓存缺失的 Token 明确记为 unknown，不估算。

Planner 缓存新增 Token usage 与 response model。Agent Trace 的 Planning 节点记录 Planner usage，
Answer 节点记录 attempts、最终 outcome 和 Token usage。评估缓存命中时仍采用缓存内原始 generation
latency，避免把毫秒级缓存查找误报为线上冷启动延迟。运行中断可从逐 Case Agent/Judge 缓存继续。

### 首轮真实结果

| Variant | Cross Complete Papers@10 | Answerability | Correctness | Faithfulness | Citation Completeness | Strict Run | Workflow P50/P95 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Fixed Hybrid RRF | 0.00% | 75.00% | 71.88% | 68.75% | 71.88% | 75.00% | 3.4 / 24.5 s |
| Fixed Hybrid + Reranker | 66.67% | 87.50% | 87.50% | 78.12% | 81.25% | 87.50% | 3.1 / 22.9 s |
| Oracle Route | **100%** | **100%** | 96.88% | **96.88%** | **100%** | **100%** | 3.4 / 17.1 s |
| LangGraph Agent | **100%** | **100%** | **100%** | **96.88%** | 96.88% | **100%** | 18.1 / 58.5 s |

Agent 的 Router、Task Count、Task Coverage、Strategy 和 Citation Validation 均为 100%；Title
Leakage 与 Unnecessary Revision 均为 0%。两条语料外题都正确拒答，没有触发无意义 Revision。
三条跨论文比较题全部召回两篇目标论文，使 Cross Complete Papers@10 达到 100%。

唯一 Retrieval 回退是 `AE-016` Exact Page Recall 只有 50%：Toolformer 命中目标页，ReAct 虽
命中目标论文但页码为 4/26 而非严格标注的 1-3。回答仍通过 Strict Run，但 Judge 对
Faithfulness/Citation Completeness 给出 3/4，说明 Paper coverage 修复不等于页级 Evidence 最优。

### 结论、限制与下一步

本轮证明 Agent 的主要价值不是“会生成答案”，而是自动识别跨论文任务并调用 Coverage，从而
达到 Oracle 的论文覆盖和 Strict Run；Fixed RAG 在同一问题集上无法稳定做到这一点。但 Agent
没有超过 Oracle，且付出明显延迟和调用成本：Planning P50/P95 为 `10.7/14.7 s`，Workflow
P50/P95 为 `18.1/58.5 s`，模型调用数为 23，高于 Oracle 的 14。

因此下一步不增加 Memory、Web Search 或更多 LangGraph 节点，而是做 Planner 模型消融：固定
这 8 条问题和 Planner Prompt，对比 `gpt-5.5` 与 `deepseek-v4-flash` 的 Router、Task Count、
Facet Coverage、结构成功率、Title Leakage、Token 与 Planning latency。只有较快模型保持路由
质量时才替换线上 Planner。Revision Recovery 当前为 N/A，因为 v1 没有必须 Revision 的真实
Case；不能把 0 次 Revision 宣称为 Recovery 能力。

正式产物：

- `evals/datasets/agent_runtime_v1.jsonl`
- `scripts/run_agent_evaluation.py`
- `evals/baselines/agent_runtime_v1.json|md`
- `evals/diagnostics/agent_runtime_v1.json`

## 29. 2026-08-18：Planner 模型消融 v1

### 开发原因与实验控制

Agent Runtime v1 的 Planning P50/P95 为 `10.7/14.7 s`，是线上延迟的主要来源。普通任务模型
`deepseek-v4-flash` 更快，但不能只凭单次 Smoke 替换关键 Planner；路由错误会直接改变
Hybrid/Coverage 策略，影响后续全部 Evidence。

本轮固定 Agent Evaluation 的 8 条问题，只比较 Planner，不执行 Retrieval、Answer 或 Judge。
两组严格共享：

- `research_planner_v1` System/User Prompt 和 `max_tokens=1800`。
- 相同的 Pydantic Schema、最多 3 次结构重试。
- Corpus v2 slug、完整标题和标题简称泄漏审计。
- Question Type、Task Count 和人工 Task Facet 金标。

`gpt-5.5` 读取 Agent Evaluation 已保存的原始 generation latency；DeepSeek 新生成 8 条 Plan，
每条结果立即写入独立缓存。缓存命中只复用原始延迟，不把缓存读取时间作为模型延迟。Planner
缓存和 Trace 同时新增 `attempts`，用于区分一次成功和经过 Schema 重试后成功。
部分 GPT 历史缓存早于 Token/attempts tracing，因此 GPT Token 只报告已知总量，旧记录的
attempts 使用兼容默认值 1；这些字段不用于本轮质量门槛或模型推荐。

### 质量门槛与结果

替换模型必须同时满足：Planning Success、Router、Task Count、Task Coverage 全部为 100%，
Title Leakage 为 0%。结果如下：

| Model | Success | Router | Task Count | Task Coverage | Leakage | Schema Retry | P50/P95 | Calls | Gate |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| gpt-5.5 | **100%** | **100%** | **100%** | **100%** | **0%** | **0%** | 10.7 / 14.7 s | 8 | Pass |
| deepseek-v4-flash | **100%** | 87.50% | 87.50% | 93.75% | **0%** | 12.50% | **5.1** / 23.9 s | 9 | Fail |

DeepSeek P50 比 GPT 低约 52.6%，但 P95 高约 62.5%。原因集中在 `AE-019`：它第一次尝试未通过
Planner 校验，第二次把“当前语料中是否报告某结果”判成 `single_paper / 1 task`，总耗时约
23.9 秒。当前 Runtime 对语料级缺失验证采用多策略检索，因此金标为 `cross_paper / 2 tasks`。
这一题本身具有标签敏感性：一次全库 Query 也有逻辑合理性，但其缺失结论的检索置信度低于正文
与实验表格两路交叉核验；报告保留该争议，不事后放宽门槛。

`AE-018` 最初被词面指标判为 50% Coverage。人工审计发现 DeepSeek 实际完整生成了两个任务，
第二路使用英文 `language-model controller / AI models / multimodal requests`。这是金标 aliases
缺失而不是模型错误，补充英文别名后恢复为 100%。该修订只修正确定性匹配，不改变 Plan 或
Question Type。

### 当前决策

生产 Planner 继续使用 `gpt-5.5`。DeepSeek 虽显著降低典型延迟，但尚未达到关键路由的零回退
门槛，尾延迟也没有优势。当前不加入“DeepSeek 失败后 GPT fallback”，因为语义上错误但结构
合法的 Plan 不会触发 Schema fallback。

下一步若继续优化 Planner，应先扩大到至少 30-50 条独立路由题，加入 single intent、
comparison、corpus-wide verification 和 label-sensitive 边界题，再评估：

- DeepSeek primary + 显式 corpus-verification 规则；
- 轻量确定性 Router 选择 DeepSeek/GPT；
- 针对 Route 设计单独分类 Prompt，再由普通模型生成 Task。

正式产物：

- `scripts/run_planner_ablation.py`
- `evals/baselines/planner_model_ablation_v1.json|md`
- `evals/diagnostics/planner_model_ablation_v1.json`
- DeepSeek Plan 缓存：`data/agent/research_plans_v1_deepseek-v4-flash.jsonl`

## 30. 2026-08-18：Planner/Router Evaluation Dataset v2

### 扩大原因

Planner 模型消融 v1 只有 8 条问题，其中 `AE-019` 已表现出明显标签敏感性。用该规模直接决定
模型切换或设计分层 Router，容易把单题标注偏好误当成模型能力。因此本轮先扩大人工可审查的
Planner/Router 数据集，不新增模型调用。

没有简单复制 Answer Evaluation Schema。Planner 评估新增独立的 `PlannerRoutingCase`：

- `category`：问题类别，而不是论文内容类别。
- `expected_question_type`：当前 Runtime 的严格 Route 策略。
- `acceptable_question_types`：边界题允许的其他合理 Route。
- `expected_task_count` 与 `acceptable_task_counts`：分开评估严格拆分和可接受拆分。
- `expected_facets`：每个 Retrieval Task 应覆盖的独立信息需求。
- `label_sensitive` 与 `annotation_notes`：显式记录标注争议和依据。

### 数据构成

`planner_routing_v2.jsonl` 共 40 条：

| Category | Cases | 目的 |
| --- | ---: | --- |
| single_lookup | 16 | 单一方法、事实或架构定位 |
| cross_comparison | 13 | 两篇论文或两种独立机制比较 |
| multi_method_synthesis | 2 | 严格要求 3/4 个 Retrieval Task |
| corpus_verification | 5 | 全语料扫描、存在性或缺失核验 |
| routing_boundary | 4 | 多实验但单论文、显式命名比较、语料措辞歧义 |

严格 Route 分布为 `single_paper=18`、`cross_paper=22`。34 条问题原样锚定已经人工检查的
`retrieval_discovery_v2` 或 `answer_generation_v1`，另外 6 条专门补足语料级核验和边界情况。
3 条 Case 标记为 label-sensitive：

- `PR-032`：缺失事实核验可用一次全库 Query，也可用正文/表格两路交叉验证。
- `PR-035`：另一条 Corpus 外结果核验，保留同样的双标签审计方式。
- `PR-039`：答案只指向一个方法，但“这些论文中”可能触发跨论文扫描。

### 校验与当前状态

新增 `validate_planner_routing_dataset.py`，校验 Schema、Case/Question 唯一性、源 Case 存在性和
源问题逐字一致性。单元测试固定 40 条规模、五类分布、18/22 Route 分布、3 条敏感标签、34 条
源锚点，并覆盖源问题被改写时必须失败。

本轮只完成数据集和校验，没有运行 80 次 Planner 调用。下一步先让 Planner Ablation Runner
同时报告 Strict、Acceptable 和排除 label-sensitive 的 Core 指标，再运行 GPT/DeepSeek v2；
不能直接沿用 v1 的单一严格准确率，否则新 Schema 中的边界标注没有实际作用。

正式产物：

- `evals/datasets/planner_routing_v2.jsonl`
- `src/paper_research_copilot/evaluation/planner_datasets.py`
- `scripts/validate_planner_routing_dataset.py`
- `tests/unit/test_planner_datasets.py`

## 31. 2026-08-18：Planner Model Ablation v2

### 实验目的与实现

v1 只有 8 条问题，单个标签敏感 Case 足以显著改变结论。本轮将 Runner 接入 40 条
`planner_routing_v2.jsonl`，在相同 `research_planner_v1` Prompt、Pydantic Schema、最多 3 次
重试和 Corpus alias 泄漏审计下，真实对比 `gpt-5.5` 与 `deepseek-v4-flash`。

新增三层口径：

- Strict：只接受唯一 `expected_question_type/task_count`，观察模型是否遵循当前 Runtime 策略。
- Acceptable：对 3 条 label-sensitive Case 接受人工预先标注的其他合理 Route/Task Count。
- Core：排除 label-sensitive Case，衡量 37 条无争议样本上的路由与拆分能力。

同时按五类问题分组，并报告 generation P50/P95、attempts、Token、Title Leakage 和逐题 Plan。
缓存记录原始 generation latency；复跑命中缓存时不把本地查找耗时当成模型延迟。

### 评估器审计与修正

首版 Gate 只要求 Core Facet Coverage=100%。真实结果中，DeepSeek 的 `PR-032` 采用了预先允许的
`single_paper / 1 task`，Route 和 Task Count 都属于 Acceptable，但只覆盖“直接检索数值”，遗漏
“从实验表格或评测段落复核”。旧 Gate 因该题是 label-sensitive 而误判通过。

这里的歧义只涉及如何路由和拆分，不代表允许漏证据。因此在不修改金标、不修改模型输出的
前提下，将 Gate 修正为全体 Case 的 Facet Coverage 必须为 100%，并新增 Boundary Coverage
指标和回归测试。修正后 DeepSeek 不再被错误推荐。

### 真实结果

| Model | Strict Route/Task | Acceptable Route/Task | Core Route/Task/Coverage | Overall Coverage | Retry | P50/P95 | Gate |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| gpt-5.5 | 95% / 95% | 97.50% / 97.50% | 97.30% / 97.30% / 100% | 100% | 0% | 10.7 / 13.6 s | Fail |
| deepseek-v4-flash | 95% / 95% | 100% / 100% | 100% / 100% / 100% | 98.75% | 5% | 5.0 / 10.8 s | Fail |

两组 80 个 Case 均成功生成 Plan，Title Leakage 都为 0%。GPT 共 40 次调用，已知 Input/Output
Token 为 `9361/10876`；DeepSeek 因两次 Schema Retry 共 42 次调用，Token 为 `12657/17471`。
不同 Provider 的 Tokenizer 口径不完全等价，因此 Token 只作各自成本记录，不直接比较模型效率。

### 失败 Case 审计

- `PR-016 / GPT`：同一 WebArena 论文、同一基准中的 GPT-4 Agent 与人类成功率，被当成两个独立
  对象拆成 `cross_paper / 2 tasks`。Facet 覆盖完整，但属于不必要的过度拆分和额外检索成本。
- `PR-039 / GPT`：因为“这些论文中”选择跨论文扫描；该题预先标为 label-sensitive，因此属于
  Strict 差异，Acceptable 指标通过，Facet 完整。
- `PR-032 / DeepSeek`：选择可接受的单任务 Route，但只覆盖直接数值检索，缺少表格/评测段落复核；
  同时发生一次 Schema Retry，是本轮不能切换模型的主要风险。
- `PR-035 / DeepSeek`：同样把语料级缺失验证压缩为单任务，但 Query 同时包含正文和表格，两个
  Facet 都覆盖，因此只属于 Strict 差异。
- `PR-030 / DeepSeek`：第一次结构输出未通过校验，第二次成功生成 3 个完整 Task，造成约
  `24.1 s` 的尾延迟。

### 当前决策与下一步

DeepSeek 在 37 条 Core Case 上全部正确，P50 比 GPT 低约 53.5%，证明它有成为普通 Planner 的
潜力；但语料级缺失验证会倾向压缩任务，且结构重试会形成长尾。GPT 的错误是过度拆分而不是
漏证据，在当前 Research 场景更保守。因此两者均未通过零回退 Gate，生产 Planner 暂时继续
使用 `gpt-5.5`。

下一步优先对 Planner 做最小定向改进：为 corpus-wide verification 增加显式路由信号或专用
Prompt，要求计划同时覆盖直接正文检索和结果表/评测段落复核；然后只在相关 Case 加若干稳定性
重复，并重新运行 v2。暂不增加 Memory、Web Search 或更多 LangGraph 节点。

正式产物：

- `scripts/run_planner_ablation_v2.py`
- `src/paper_research_copilot/evaluation/planner_ablation_v2.py`
- `tests/unit/test_planner_ablation_v2.py`
- `evals/baselines/planner_model_ablation_v2.json|md`
- `evals/diagnostics/planner_model_ablation_v2.json`

## 32. 2026-08-18：Corpus Verification Prompt Ablation v2

### 优化原因与边界

Planner Model Ablation v2 表明 DeepSeek 在 37 条 Core Case 上路由能力足够，但会把语料级
存在性/缺失核验压缩成一个直接数值 Query，遗漏独立复核通道。本轮不增加 LangGraph 节点、
Memory 或检索工具，只修改 Planner 的规划契约，并用全集回归与定向稳定性验证是否值得切换模型。

新增 `research_planner_corpus_verification_v2` Prompt Profile：

- 语料级存在性/缺失核验使用 `cross_paper / 2 tasks`：正文直接检索，以及 results/evaluation/
  tables/figures/appendices 独立复核。
- 同一论文、同一基准中的多个事实或指标保持 `single_paper / 1 task`，避免仅因请求两个数值而
  过度拆分。
- “这些论文中哪一种方法”本身不等于语料缺失核验，防止所有语料措辞都被强制跨论文。
- Corpus-wide synthesis 将“定位工作”和“提取属性”视为不同信息需求。

`CachedResearchPlanner` 现在可显式选择 Prompt Version，版本与完整 Prompt 一同进入缓存 Hash。
缓存加载和写回保留其他 Prompt Version 的记录，因此 v2 实验不会覆盖 v1 历史缓存。默认值仍为
`research_planner_v1`，Agent Factory 没有切换生产配置。

### 实验设计

第一阶段在 40 条 `planner_routing_v2` 全集运行一次 DeepSeek Prompt v2，检查定向修改是否导致
其他类别回退。第二阶段选择 10 条高风险样本，使用三个独立缓存进行真实重复：5 条 corpus
verification、4 条 routing boundary，以及 GPT 曾过度拆分的 `PR-016`。r1 复用同一次全集生成，
r2/r3 是独立调用，共新增约 60 次模型调用。

生产切换不仅要求全集和三次定向质量 Gate 全部通过，还要求 Prompt v2 的 Retry 不高于
DeepSeek v1，并且 P95 低于当前生产 GPT。这样可避免“准确率提高但线上长尾恶化”仍被推荐。

### 全集结果

| Variant | Core Route/Task/Coverage | Overall Coverage | Retry | P50/P95 | Gate |
| --- | ---: | ---: | ---: | ---: | --- |
| Production GPT Prompt v1 | 97.30% / 97.30% / 100% | 100% | 0% | 10.7 / 13.6 s | Fail |
| DeepSeek Prompt v1 | 100% / 100% / 100% | 98.75% | 5% | 5.0 / 10.8 s | Fail |
| DeepSeek Prompt v2 | 100% / 94.59% / 100% | 100% | 7.50% | 5.0 / 25.8 s | Fail |

Prompt v2 修复了 DeepSeek v1 的 `PR-032` 验证缺失，并保持 `PR-016` 的正确同论文单任务规划。
`PR-016` 在 DeepSeek v1 中原本已做对，因此本轮只把它作为防过度拆分 Guardrail，不计为新增
提升。全集 Facet Coverage 达到 100%，Router 也没有 Core 回退。但 `PR-034/036` 被拆成 3 个任务而
严格策略要求 2 个，使 Core Task Count 降到 94.59%。这两个结果仍在预先标注的 Acceptable
Task Counts 内，因此回答能力未必下降，但说明 Decomposition 不稳定，不能通过零回退 Gate。

Prompt 更长后 Input Token 明显增加。3 条发生 Schema Retry：`PR-030` 三次尝试后成功，约
44.7 秒；`PR-033/039` 两次尝试后成功，约 26 秒。Retry Rate 从 v1 的 5% 升到 7.5%，P95 从
10.8 秒恶化到 25.8 秒，失去了作为低延迟 Planner 的主要优势。

### 稳定性与 Evaluation 审计

| Repetition | Acceptable Route/Task | Coverage | Retry | Gate |
| --- | ---: | ---: | ---: | --- |
| r1 | 100% / 100% | 100% | 20% | Fail |
| r2 | 100% / 100% | 100% | 0% | Fail |
| r3 | 100% / 100% | 100% | 10% | Pass |

r1/r2 失败来自 `PR-034/036` 的 Strict Task Count 波动；r3 的严格拆分全部命中，但仍有一次
Schema Retry。三次中只有一次通过质量 Gate，不能认为 Prompt 行为稳定。

r2 的 `PR-032` 最初被确定性匹配判为 50% Coverage。人工审计原始 Plan 后发现 T2 明确要求在
“评估、结果、表格、图表或附录”中复核，只是 Golden aliases 没有覆盖中文组合“结果 表格”。
补充该 alias 后三次 Coverage 都为 100%。这个修订只修复词面假阴性，没有修改 Route、Task
Count、问题文本或模型 Plan。

### 当前决策

Production switch review 为 False。Prompt v2 证明定向规划契约能修复 corpus verification，
但也证明继续向单个 Prompt 叠加规则会干扰 Task Count，并增加结构失败和尾延迟。生产继续使用
`gpt-5.5 + research_planner_v1`，实验 Profile 保留用于后续消融，不接入 Agent Factory。

下一步不在同一 40 条数据上继续 Prompt 微调，避免过拟合。更合理的工程方向是把 Router 与
Task Generator 分开：轻量 Router 只输出稳定的 intent/strategy，Task Generator 再根据显式
策略生成 Query；在此之前还应保存每次失败 attempt 的错误类型和截断响应，定位 Schema Retry
究竟来自 JSON、长度还是字段校验。

正式产物：

- `scripts/run_planner_prompt_ablation.py`
- `src/paper_research_copilot/agent/planner.py`
- `evals/baselines/planner_prompt_ablation_v2.json|md`
- `evals/baselines/planner_prompt_ablation_v2_full.json|md`
- `evals/baselines/planner_prompt_ablation_v2_stability.json|md`
- `evals/diagnostics/planner_prompt_ablation_v2_full.json`
- `evals/diagnostics/planner_prompt_ablation_v2_stability.json`

## 33. 2026-08-18：Planner Attempt Observability 与 Retry 根因诊断

### 开发原因

Prompt Ablation v2 中 DeepSeek 出现 7.5% Schema Retry，`PR-030` 最慢约 44.7 秒。但当时缓存只
记录最终成功响应、总 attempts 和最终 Usage，无法判断前序失败来自非法 JSON、Pydantic Schema、
Task Count、标题泄漏、输出截断还是 Provider 异常。若在不知道根因时直接拆 Router 或继续改
Prompt，可能增加架构复杂度却没有解决尾延迟。

### 实现措施

新增 `PlannerAttemptTrace`，每次调用记录：

- attempt index、outcome、finish reason、content length 和 latency；
- response model 与 Input/Output/Total Token；
- error type、error message 和 raw response。

新增 `PlannerGenerationTrace`，汇总 operation、question、model、Prompt Version、input SHA256、
retry triggered/recovered 和 final outcome。Outcome 使用确定性阶段分类：

- `empty`、`truncated`、`provider_error`；
- `invalid_json`、`schema_validation_error`、`invalid_task_shape`；
- `invalid_plan`、`unchanged_revision`、`title_leakage`；
- `accepted`。

Trace 随 Planner Cache 写入 `data/agent/`，该目录已被 Git ignore。正式 Diagnostics 删除
`raw_response`，只记录 SHA256、长度、错误和成本。全量 Token Usage 改为累计所有 attempts，
修复过去只统计最终成功调用的低估。旧缓存的 `generation_trace` 字段有兼容默认值，不要求迁移。

单元测试覆盖 invalid JSON、Payload Schema、Task Shape、empty、truncated、Provider Error、
Title Leakage、Retry Recovery、Token 聚合、缓存 Trace 恢复和脱敏 SHA256。

### 真实诊断实验

使用 `research_planner_corpus_verification_v2`、DeepSeek、`max_tokens=1800` 和全新独立缓存，对
历史长尾 `PR-030/033/039` 各重复 3 次，共 9 个 Plan：

| Rep | Case | Attempts | Outcomes | Latency |
| ---: | --- | ---: | --- | ---: |
| 1 | PR-030 | 1 | accepted | 11.2 s |
| 1 | PR-033 | 2 | truncated -> accepted | 24.3 s |
| 1 | PR-039 | 1 | accepted | 5.2 s |
| 2 | PR-030 | 1 | accepted | 13.1 s |
| 2 | PR-033 | 1 | accepted | 7.7 s |
| 2 | PR-039 | 1 | accepted | 9.8 s |
| 3 | PR-030 | 3 | truncated -> truncated -> accepted | 49.8 s |
| 3 | PR-033 | 2 | truncated -> accepted | 25.8 s |
| 3 | PR-039 | 1 | accepted | 13.8 s |

最终 Success 为 100%，但 3/9 Plan 触发重试，Retry Rate 33.33%，P50/P95 为 `13.1/49.8 s`。
共 4 个失败 attempts，全部是 `finish_reason=length`；4/4 的 Output Token 等于配置上限 1800，
其中 3 次 Provider 返回空 content，1 次返回 322 字符的半段 JSON。没有出现 invalid JSON、
Schema Validation、Task Shape、Title Leakage 或 Provider Error。

### 结论与下一步

本次实验证明当前长尾的直接原因是输出截断，不是 Route/Task Schema 不稳定。尤其 `PR-030`
连续两次用相同 Prompt 原样重试仍被截断，说明当前 Retry 策略没有针对失败类型采取恢复措施。
因此暂缓 Router/Task Generator 拆分，先做更小的 Truncation Recovery 消融：

1. `max_tokens=1800` + 原样重试，作为当前 Baseline。
2. `max_tokens=1800` + 截断后追加 compact JSON 指令，限制 rationale/query/goal 长度。
3. 提高 `max_tokens`，但不改 Prompt，验证是否只是上限不足以及成本变化。

三组必须保持 Route、Acceptable Task Count、Facet Coverage 和 Leakage 不回退，再比较 Retry Rate、
P95 与 Token。只有结构恢复解决后，若 Task Count 仍不稳定，才进入 Router/Task Generator 拆分。

正式产物：

- `scripts/run_planner_retry_diagnostics.py`
- `src/paper_research_copilot/agent/planner.py`
- `tests/unit/test_agent_planner.py`
- `tests/unit/test_planner_retry_diagnostics.py`
- `evals/baselines/planner_retry_diagnostics_v1.json|md`
- `evals/diagnostics/planner_retry_diagnostics_v1.json`
- 本地 Raw Trace：`data/agent/planner_retry_diagnostics_v1_raw.jsonl`（ignored）

## 34. 2026-08-18：Planner Truncation Recovery Ablation

### 优化原因

Attempt Diagnostics 已证明历史 Schema Retry 实际都是 `finish_reason=length`，且 Output Token
精确达到 1800 上限。原策略不区分失败类型，只把相同 Prompt 原样重试；`PR-030` 曾连续截断
两次，说明重试能够偶然恢复，却没有主动减少再次截断的概率。本轮先解决这一确定根因，继续
暂缓 Router/Task Generator 拆分。

### 实现措施

`CachedResearchPlanner` 新增 `retry_mode`：

- `same_prompt` 保持原行为和旧缓存 Hash 兼容。
- `compact_json` 只在上一 attempt 为 `truncated` 时追加紧凑 JSON 指令，限制 rationale、query、
  goal 长度并禁止分析、Markdown 和额外字段；其他错误后仍使用原 Prompt。
- 非默认 `compact_json` 进入缓存 Hash，避免与 Baseline 串用；Trace 和 Cache Record 同时记录
  retry mode。

单元测试覆盖截断触发、非截断不触发、原样 Prompt、缓存隔离、旧缓存兼容和 Trace 恢复。
实验 Runner 按 repetition → case → strategy 交错调用，减少 Provider 时段波动；每个样本实时
保存 ignored raw trace，正式 Diagnostics 删除响应原文，只保留 SHA256。

实验固定 DeepSeek、Prompt v2 和 `PR-030/033/039`，比较：

1. `same_1800`：1800 Token，截断后原样重试。
2. `compact_1800`：1800 Token，截断后追加紧凑 JSON 指令。
3. `same_2400`：2400 Token，截断后原样重试。

每题每组独立重复 3 次，共 27 个 Plan，最多 3 attempts。质量 Gate 要求 Success、Acceptable
Route、Acceptable Task Count、Facet Coverage 均为 100%，Title Leakage 为 0%。

### 实验结果

| Strategy | Success | Route/Task Acceptable | Coverage | Retry/Recovery | Truncated | P50/P95 | Output Tokens | Gate |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| same_1800 | 88.89% | 88.89% / 88.89% | 88.89% | 55.56% / 80.00% | 7 | 25.1 / 60.9 s | 19,743 | Fail |
| compact_1800 | 88.89% | 77.78% / 77.78% | 88.89% | 11.11% / 0.00% | 3 | 11.7 / 64.2 s | 14,609 | Fail |
| same_2400 | **100%** | **100% / 100%** | **100%** | 44.44% / **100%** | 5 | 12.1 / 68.8 s | 22,373 | **Pass** |

两个终态失败均为连续三次截断：`same_1800 / PR-030 r1` 和
`compact_1800 / PR-039 r1`。Compact 失败样本在追加指令后仍连续用满 1800 Token，说明自然语言
长度约束不能可靠控制该模型的隐式推理预算。它的 `PR-030 r3` 还在首轮生成 single-paper Route；
由于首轮尚未追加 Compact 指令，与 Baseline Prompt 完全相同，这属于随机波动，不能宣称是
Compact 策略造成的质量回退。

`same_2400` 是唯一通过 Gate 的组，证明提高输出上限能改善三次机会内的最终恢复率。但它仍有
4/9 Plan 触发 Retry、5 个截断 attempts，P95 达 68.8 秒且总 Output Token 最高。也就是说，
2400 解决了本轮“最终失败”，没有解决“过度生成与长尾”。仅看 P50 会错误地认为问题已解决。

### 当前决策与下一步

把 `same_2400` 记录为 **DeepSeek + Prompt v2 的候选恢复配置**，但不修改当前生产默认：生产仍是
`gpt-5.5 + Prompt v1`，本实验没有验证它在 1800/2400 下的质量与成本；全局修改会把局部结论
错误外推到不同模型和 Prompt。

下一步优先增加第四个最小候选 `compact_2400`，验证更高上限与截断后紧凑重试能否同时保持
100% Success 并降低二次截断；若仍然长尾，再考察 Provider 的 JSON Schema/structured output、
更短的 Planner 输出 Schema，最后才回到 Router/Task Generator 拆分。样本量也应从 3 Cases ×
3 次扩大，避免根据单次随机 Route 波动做生产切换。

正式产物：

- `scripts/run_planner_truncation_ablation.py`
- `src/paper_research_copilot/agent/planner.py`
- `tests/unit/test_agent_planner.py`
- `tests/unit/test_planner_truncation_ablation.py`
- `evals/baselines/planner_truncation_recovery_v1.json|md`
- `evals/diagnostics/planner_truncation_recovery_v1.json`
- 本地 Raw Trace：`data/agent/planner_truncation_recovery_v1_raw.jsonl`（ignored）

## 35. 2026-08-18：Compact 2400 验证与停止条件

### 实验目的

上一轮 `compact_1800` 没有恢复唯一的截断样本，但无法排除 1800 Token 本身过低。本轮只做
一个最小验证：重新交错对比 `same_2400` 与 `compact_2400`，仍用 3 个历史长尾 Case、每题
3 次，共 18 个 Plan。预先设定停止条件：若 Compact 不能同时保持 100% Success 并减少连续
截断/P95，就不再继续修改 Prompt 措辞或 Token 组合。

Runner 增加 `--strategy`，可以只执行指定策略；默认仍保留上一轮三组，避免原实验命令的行为
发生变化。`compact_2400` 与其他非默认模式继续使用独立缓存键和脱敏 Diagnostics。

### 结果

| Strategy | Success | Acceptable Route/Task | Retry/Recovery | Truncated | P50/P95 | Output Tokens | Gate |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| same_2400 | 100% | 88.89% / 77.78% | 0% / N/A | 0 | 10.2 / 20.2 s | 9,229 | Fail |
| compact_2400 | 88.89% | 77.78% / 77.78% | 33.33% / 66.67% | 6 | 16.3 / 73.1 s | 25,735 | Fail |

`compact_2400 / PR-033 r1` 连续三次截断并终态失败，`PR-033 r2` 也连续截断两次后才恢复；
Compact 没有降低二次截断，反而使本轮模型调用数从 9 增至 14，Output Token 约为对照的
2.8 倍。该策略明确不采用。

`same_2400` 本轮 9/9 首轮成功，说明截断具有明显随机性；但 `PR-030 r3` Route 错误、
`PR-033 r3` Task Count 错误，依然没有通过质量 Gate。由于 Compact 指令只在截断后出现，这些
首轮质量波动与 Compact 无关，暴露的是 DeepSeek Prompt v2 本身的规划稳定性。

### 决策

达到停止条件：不继续做更多 Token 上限、Compact 文案或重试次数微调。当前生产配置保持
`gpt-5.5 + Prompt v1`。DeepSeek Prompt v2 的低成本切换实验暂告结束；只有未来确实需要替换
生产 Planner 时，再把结构化输出或 Router/Task Generator 分离作为独立架构任务评估。

下一步回到求职作品的主线工程价值：完善 FastAPI/流式任务接口、Agent 运行过程可观察性和可
展示的前端交互，而不是继续优化这 3 条 Planner Case。

正式产物：

- `evals/baselines/planner_compact_2400_ablation_v1.json|md`
- `evals/diagnostics/planner_compact_2400_ablation_v1.json`
- 本地 Raw Trace：`data/agent/planner_compact_2400_ablation_v1_raw.jsonl`（ignored）

## 36. 2026-08-18：FastAPI 异步任务与 SSE 可观察性

### 开发原因

项目已经具备完整 RAG、LangGraph Runtime 和多层 Evaluation，但主要入口仍是同步 CLI。面试展示
时只能看到命令等待后一次性输出结果，无法观察 Planner、Retrieval、Evidence Gate、Revision
和 Citation Validation 的执行过程，也没有稳定接口供前端消费。相比继续微调少量 Planner Case，
把现有能力产品化能更直接体现模块边界、状态管理和可观察性。

### 模块与契约

新增三层：

- `api/models.py`：公开 Pydantic 契约，定义 Research Request、Task Status、SSE Event、Task
  Result 和 Health。
- `api/service.py`：线程安全的内存任务仓库、单 Worker Executor、Runtime 生命周期和事件队列。
- `api/app.py`：FastAPI Route、HTTP 错误映射、SSE 编码和 Uvicorn 入口。

公开接口为：

- `POST /api/v1/research`：验证问题并返回 HTTP 202 与稳定 Task ID。
- `GET /api/v1/research/{task_id}`：返回 queued/running/succeeded/failed 和最终 AgentResult。
- `GET /api/v1/research/{task_id}/events`：实时输出 task 与 LangGraph node 事件。
- `GET /health`：惰性构建 Runtime，实际检查凭据、Corpus 和非空 Qdrant Collection。

`ResearchAgentRuntime.run()` 增加可选 `event_callback`。每个节点构造原生 `AgentEvent` 后立即通知
任务服务；不传 Callback 时 CLI、Evaluation 和既有调用行为不变。API 没有访问 `_graph` 或复制
LangGraph 节点，因此 Runtime 仍是研究流程的唯一实现。

### 工程边界

当前使用 `ThreadPoolExecutor(max_workers=1)`。这是刻意选择：本地 Qdrant Client 与 JSONL Planner
Cache 尚未声明多写并发安全，作品演示也不需要先引入 Redis/Celery。Task Record 和事件列表通过
`Condition` 保护，SSE 支持 heartbeat、严格递增 sequence 和 `after` 游标。应用关闭时等待当前
任务并幂等关闭 Runtime。

明确不包含：任务持久化、跨进程恢复、多 Worker、取消、认证和公网部署。后续如需扩展，应保留
HTTP/Pydantic 契约，将 Service 实现替换为 Redis + Worker，而不是把队列逻辑写入 Route。

### 测试

新增测试覆盖：

- Runtime 节点事件 Callback 顺序与最终 Trace 完全一致。
- POST 稳定返回 queued 快照，后台任务最终成功并返回完整 AgentResult。
- SSE 包含 queued、started、多个 agent_node 和 terminal event。
- Provider/Runtime 异常进入 failed 状态并形成终态事件。
- Health Runtime Probe、404、短问题和未知字段校验。

完整回归为 143 passed，Ruff、mypy 和 `pip check` 通过。随后在 Conda 环境安装 editable
entrypoint，启动真实 Uvicorn 并完成两层 Smoke：

- `/health` 返回 `ok`，Runtime、Corpus v2 与 `agent_seed_v2_bge_m3_chunking_v1` 均 Ready；
  `/openapi.json` 包含四个预期路径。
- 通过 HTTP 提交真实研究问题，POST 返回 queued；SSE 返回 queued、started、5 个 agent_node
  和 succeeded，共 8 个事件；最终为 cross-paper / 2 Tasks / 10 Evidence / answered / 1 Citation。

这证明 API 不只是 Fake Runtime 单测通过，真实 gpt-5.5 Planner、Qdrant Retrieval、DeepSeek
Answer 与 Citation Validation 也已从 HTTP/SSE 链路贯通。

正式产物：

- `src/paper_research_copilot/api/models.py`
- `src/paper_research_copilot/api/service.py`
- `src/paper_research_copilot/api/app.py`
- `docs/api.md`
- `tests/unit/test_api.py`

## 37. 2026-08-18：React Research Workspace 与浏览器验收

### 开发目标

FastAPI 已让 Agent 能以任务方式运行，但面试演示仍需要手工调用 HTTP 或观察原始 SSE。前端的
目标不是增加新 Agent 能力，而是把已经可评估、可追踪的执行过程组织成一个可以直接操作的研究
工作台。范围限制为当前 Task，不增加登录、历史会话、状态库或主题系统。

### 技术与模块

在 `frontend/` 新建 React 19 + TypeScript + Vite 工程，使用 Lucide Icons 和原生 EventSource。
主要边界：

- `types.ts` 映射后端 Health、Task、AgentResult、Evidence 和 SSE 契约。
- `api.ts` 负责 HTTP 错误映射、任务创建/查询与命名 SSE 订阅。
- `App.tsx` 负责当前 Task 生命周期、sequence 去重、终态 Snapshot 和轮询降级。
- Components 分为 System Header、Research Sidebar、Agent Timeline 和 Research Result。

未引入 Redux/Zustand、UI Framework 或 Markdown HTML Renderer。当前状态规模只涉及一个任务，
React State 足够；Answer 使用纯文本渲染，避免执行模型生成的 HTML。

### 交互设计

桌面采用三栏工作区：左侧输入与 Task Metrics，中间按节点展示 outcome、latency 和关键 details，
右侧提供 Report、Evidence、Plan 三个 Tab。配色以白色、灰色、绿色主操作、蓝色 Trace 和独立
成功/警告/失败状态组成，不使用营销 Hero、装饰渐变或嵌套 Card。

SSE 终态到达后主动关闭 EventSource，避免正常 EOF 被浏览器当成断线反复重连；事件按 sequence
去重。真正的网络异常会切换为 2 秒 Task Polling，终态自动停止。Vite 通过同源代理访问后端，
开发阶段不扩大 CORS。

### 浏览器发现与修复

使用 in-app Browser 对正在运行的真实前后端进行验收：

- 1280×720：三栏宽度为 330/350/600px，Body `scrollWidth == clientWidth == 1280`，无横向
  溢出。
- 真实任务：SSE 先显示 5 个进行中事件，最终得到 8 个事件；Task 为 succeeded、2 Tasks、
  10 Evidence、1 Citation，Report 正常显示。
- Evidence Tab 渲染 10 个 Chunk，Result Panel `scrollWidth == clientWidth == 600`；Plan Tab
  正确显示 Route、Rationale、2 个 Research Tasks 和 Assessment。
- 390×844 移动视口：三个 Panel 均为 375px 宽，Body 无横向溢出，Tab、Metric Grid、论文标题
  与 Evidence 文本没有重叠。

第一次实现把 Citation Source 写成 `#evidence-C3` 锚点，但 Evidence DOM 只在对应 Tab 选中时
存在，点击报告引用无法定位。修复为命令按钮：先切换 Evidence Tab，再滚动至目标 Chunk；浏览器
复测确认 selected Tab 为 Evidence，目标 C3 位于 Result View 顶部并获得 `:target` 高亮。

### 验证

- Vitest：2 passed，覆盖 SSE sequence 去重/排序与时间/节点格式化。
- TypeScript strict typecheck：通过。
- Vite production build：通过，JS gzip 约 67.7 kB，CSS gzip 约 3.3 kB。
- `npm audit --omit=dev`：0 vulnerabilities。

正式产物：

- `frontend/src/App.tsx`
- `frontend/src/components/`
- `frontend/src/api.ts`
- `frontend/src/types.ts`
- `frontend/src/styles.css`
- `frontend/package.json` 与 `package-lock.json`
- `docs/frontend.md`

## 38. 2026-08-18：单进程生产演示与一键启动

### 开发原因

前后端开发模式需要分别启动 Uvicorn 和 Vite，面试演示依赖两个终端与开发代理。系统核心能力已经
完整，此时继续增加零散算法的收益低于改善可运行性，因此本阶段将 React Production Build 与
FastAPI 收口为一个同源服务，让作品能够通过一个命令启动。

### 实现措施

- FastAPI 在 `frontend/dist/index.html` 存在时挂载 Vite 的 `/assets` 静态目录，并直接提供首页。
- 非后端路径找不到实际静态文件时回退到 `index.html`，为后续前端路由保留 SPA 能力。
- `/api`、`/health`、`/docs`、`/redoc` 和 `/openapi.json` 被保留为后端命名空间。不存在的 API
  仍返回 JSON 404，不会被 SPA 回退伪装成 HTTP 200 页面。
- 静态文件访问在解析后验证仍位于 `frontend/dist` 内，拒绝路径越界。
- 没有 Production Build 时 API 仍可独立启动，静态托管是可选交付层，不改变 Agent Runtime。
- 新增 `scripts/start_demo.ps1`：检查 Node/npm 和 Python Entry Point，缺少 `node_modules` 时执行
  `npm ci`，随后完成 TypeScript 检查、Vite Build，并启动 `paper-research-api`。
- README、API 和前端文档同时增加生产演示与开发模式的明确边界。

### 验证结果

- 后端完整回归：144 passed；Ruff 通过；mypy 55 个模块通过。
- 前端：Vitest 2 passed；TypeScript strict typecheck 通过；Vite Production Build 通过。
- 构建大小：JS 213.48 kB（gzip 67.67 kB），CSS 13.11 kB（gzip 3.31 kB）。
- 独立端口真实 Uvicorn Smoke：`/`、`/research/demo`、哈希 JS、`/openapi.json` 均返回 200；
  `/api/v1/not-a-route` 返回 `application/json` 404。
- `start_demo.ps1 -SkipBuild` 已真实启动完整服务；同源首页与哈希 JS 返回 200，Runtime Health 为
  `ok`，Corpus v2、Qdrant Collection 和 Agent Runtime 均 Ready。

该阶段没有修改 Planning、Retrieval、Reflection、Answer 或 Evaluation 行为。项目现在具备一个命令
启动完整作品的演示路径；下一阶段进行最后一次有边界的 Corpus 扩展和对应 Evaluation 更新。

正式产物：

- `src/paper_research_copilot/api/app.py`
- `scripts/start_demo.ps1`
- `tests/unit/test_api.py`
- `README.md`
- `docs/api.md`
- `docs/frontend.md`

## 39. 2026-08-18：Corpus v3 与 Evaluation 扩展

### Corpus 验收

v3 在保留 v2 20 篇论文的基础上新增 20 篇，覆盖 Architecture/Reflection、Tool Interaction、
Web/Computer Agent、通用 Agent Evaluation、Software Engineering Agent 和 Agentic RAG。
40 个固定 revision PDF 共 216.79 MiB；spec、manifest、本地文件集合、revision、文件大小和
40 个 SHA-256 全部一致。Parse 汇总为 31 verified、9 warning、0 failed，共 1,387 页和
4,457,416 个抽取字符。

新增 20 篇首页和 10 个异常/短文本页已完成视觉核验。warning 来自图形页、公式和特殊符号文本
映射，不是文件损坏；Evaluation 避开 ExpeL 的纯图片页。重新运行 `corpus-stats --version 3`
得到 2,152 个 `chunking_v1` Chunk，0 oversized、0 duplicate ID、0 missing required
metadata，Section coverage 99.72%。

### Evaluation 版本化

- `retrieval_discovery_v3.jsonl`：100 条，前 50 条原样继承 v2，新增 30 条单论文和 20 条
  跨论文 challenge；合计 35 条 cross-paper、80 条 Hard Negative，覆盖全部 40 篇。
- `answer_generation_v2.jsonl`：32 条，31 可回答、1 拒答、7 cross-paper。AE-020 随
  tau-bench 入库转为可回答；AE-019 仍拒答，因为语料没有 GPT-5.5 在 SWE-bench Verified
  上的结果。
- `agent_runtime_v2.jsonl`：12 条，11 可回答、1 拒答、7 cross-paper，并与 Answer v2
  的 question、answerable 和 relevant pages 保持一致。

所有新增页码均通过现有 PDF 文本层检索核对；Discovery 通过 schema、唯一 ID、页码范围、
论文集合和标题泄漏校验。历史 Corpus/Evaluation 文件与已冻结 Baseline 不修改。

本阶段没有调用 Embedding、Qdrant 写入、Answer/Judge 或 Agent 模型。v3 真实检索和端到端
Baseline 应在独立 Collection 中作为下一阶段执行，当前生产演示继续使用已验证的 Corpus v2。

## 40. 2026-08-19：Corpus v3 向量化与 Retrieval Evaluation

### 优化原因

Corpus 从 20 篇扩大到 40 篇后，同主题论文数量显著增加。仅证明 PDF 可以 Parse 和 Chunk，
不能说明原有 Retriever 在更强 Hard Negative 干扰下仍然有效，因此必须使用独立 Collection
重新向量化，并在保持旧 50 题不变的前提下运行 v3 Retrieval Baseline。这样既能测量新增问题，
也能直接观察同一问题在扩库前后的性能漂移。

### 实施措施

- 使用 SiliconFlow `BAAI/bge-m3` 为 2,152 个 `chunking_v1` Chunk 生成 1024 维向量。
- 写入独立 Qdrant Collection `agent_seed_v3_bge_m3_chunking_v1`；实际点数为 2,152，距离
  度量为 Cosine，没有与 v1/v2 混写。
- 在 `retrieval_discovery_v3` 的 100 条问题上分别运行 Dense、BM25 和 Hybrid RRF，Top-K
  固定为 10。
- 新增 `scripts/build_retrieval_v3_comparison.py`，只读取已保存 Raw Results，统一报告总体、
  旧 50 题、新 50 题、新增单论文、新增跨论文以及 v2 -> v3 扩库漂移，不重复调用模型。

### 实验结果

| Strategy | Paper Recall@10 | Complete Papers@10 | Exact Page Recall@10 | Complete Pages@10 | Paper MRR | Page MRR |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| Dense | 92.00% | 85.00% | 83.92% | 74.00% | 0.8835 | 0.7264 |
| BM25 | 67.92% | 58.00% | 65.67% | 55.00% | 0.7078 | 0.5579 |
| Hybrid RRF | 92.17% | 84.00% | 86.92% | 75.00% | 0.9194 | 0.7962 |

Hybrid 相比 Dense 的 Paper Recall 基本持平，但 Exact Page Recall 高 3 个百分点，Paper MRR
和 Page MRR 也更高。BM25 在中文问题、英文论文正文的跨语言设置下经常没有有效词项匹配，
不能独立作为默认 Retriever；它的价值主要是补充公式名、指标名和精确英文术语。

同一批 DS-001..050 从 v2 扩到 v3 后，Dense 的 Paper Recall、Complete Papers、Exact Page
Recall 分别下降 `2 / 4 / 4` 个百分点；Hybrid 分别变化 `0 / 0 / -2` 个百分点，证明 Hybrid
对新增同主题干扰更稳健。

新增单论文题上，Dense 与 Hybrid 的 Complete Papers@10 都为 96.67%，Hybrid Exact Page
Recall@10 为 96.67%，高于 Dense 的 93.33%。新增跨论文题上，Dense / Hybrid 的 Complete
Papers@10 仅为 60% / 50%，Hybrid Complete Pages@10 只有 30%。这说明全局 Top-10 融合仍可能
被一个子主题占满，不能依靠 Hybrid 自动保证多个目标论文的覆盖。

### 最终决策

- 单论文 Discovery 保持 Hybrid RRF，它在页级定位、排序和扩库稳定性上优于 Dense。
- 跨论文 Agent 保持 Query Decomposition + Coverage Merge，由任务级配额显式保证覆盖。
- 不为 v3 继续做细小的 RRF/MMR 参数搜索；当前瓶颈是 multi-target coverage，不是全局排序分数。
- 生产 API 暂时继续使用通过 Answer/Agent 验证的 Corpus v2。下一步先在 v3 Collection 上运行
  Answer v2，再决定是否执行 Agent Runtime v2 评测和切换生产版本。

正式产物：

- `evals/baselines/dense_discovery_v3.json|md`
- `evals/baselines/bm25_discovery_v3.json|md`
- `evals/baselines/hybrid_rrf_discovery_v3.json|md`
- `evals/baselines/retrieval_discovery_v3_comparison.json|md`
- `scripts/build_retrieval_v3_comparison.py`

## 41. 2026-08-19：Answer v2 与 Semantic Judge

### 评估原因

Retrieval 指标只说明目标论文或页面是否进入 Top-K，不能证明模型能据此正确回答、完整引用，
也不能区分“没有召回证据”和“召回后仍错误拒答”。因此在 v3 Collection 上运行 32 条 Answer
v2，并继续使用确定性指标与独立 `gpt-5.5` Judge 两层评估。

### 实验设置与结果

- Retrieval：Fixed Hybrid RRF / Top-10。
- Answer：`deepseek-v4-flash` / `answer_v2`，空响应或截断最多 2 次尝试。
- Dataset：32 条，31 可回答、1 条 Corpus 外拒答。

| Generation Success | Answerability | Key Point Recall | Citation Paper Recall | Strict Page Recall | P50/P95 |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 96.88% | 78.12% | 71.24% | 75.81% | 74.19% | 4.0 / 25.2 s |

`gpt-5.5` Judge 对 24 条有效回答执行 Semantic Judge，对 8 条 Generation Error 或结构化拒答
执行确定性评分，Judge failures 为 0：Correctness `74.22%`、Faithfulness `73.44%`、Citation
Completeness `75.00%`、Overall `74.22%`。Judge P50/P95 为 `22.0 / 33.1 s`，因此它只用于
离线 Evaluation，不进入在线回答链路。

### 失败归因

- `AE-015` 连续两次返回 `finish_reason=length + empty content`，属于 Provider/Generation 截断。
- `AE-027` 已完整召回目标论文和标注页，但 Answer 模型仍返回 `INSUFFICIENT_EVIDENCE`，属于
  Generation 的保守拒答。
- `AE-017/018/030/031/032` 的 Fixed Hybrid 只覆盖一个比较目标，属于 multi-target Retrieval
  coverage 失败；强 LLM 不应靠参数知识补齐缺失论文。
- `AE-019` 正确拒答，说明语料扩大后 Corpus 外负例契约仍有效。

这一步的价值不在于得到一个更高总分，而是证明三层 Evaluation 能把同一个低分拆成 Retrieval、
Generation 和 Semantic Grounding 三种不同故障，避免盲目修改 Prompt 或 Retriever。

正式产物：

- `evals/baselines/answer_hybrid_rrf_v2.json|md`
- `evals/baselines/answer_hybrid_rrf_gpt55_judge_v2.json|md`
- `evals/results/answer_hybrid_rrf_v2.jsonl`
- `evals/results/answer_hybrid_rrf_gpt55_judge_v2.jsonl`

## 42. 2026-08-19：Agent Runtime v2 对照与生产切换

### 优化原因

Answer v2 已证明 Fixed Hybrid 的主要失败集中在跨论文多目标覆盖。此前 Agent Runtime 已具备
Planner、单/跨论文路由、Coverage Merge、Evidence Gate、有界 Reflection 和 Citation
Validation，但必须在同一 Corpus、Case、Answer 与 Judge 口径下证明这些编排是否带来最终答案
收益，而不是只展示流程图。

### Evaluation Harness 版本化

原 `run_agent_evaluation.py` 固定读取 v1 的 8 个 Case、Stability/Coverage 样本和 cache；直接传入
v2 Dataset 会混用不可比较的冻结结果。本轮增加：

- `--all-cases`，显式运行选定 Dataset 的全部 Case。
- `--saved-answer-results` 与 `--saved-judge-results`，让 v2 使用同一 Corpus v3 Fixed Hybrid
  结果作为对照，不复用 v1 样本。
- `--judge-cache-tag` 和独立 `--agent-cache`，隔离 v1/v2 Agent 与 Judge cache。
- 单个 Runtime 异常会写入 error cache、由确定性 Judge 记 0 分并继续，不再中断整批实验。

最后一条来自真实故障：首次运行到 `AE-032` 时，`write_report` 连续两次截断，导致旧脚本在
11/12 处退出。修复 harness 后恢复运行，前 11 条复用逐题 cache，AE-032 再次运行成功，但
Correctness 只有 `2/4`；首次截断仍作为稳定性风险保留，不能解释为完全修复。

### Agent-vs-Fixed-RAG 结果

| Variant | Paper Recall@10 | Cross Complete@10 | Answerability | Correctness | Faithfulness | Strict Run | Workflow P50 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Fixed Hybrid | 86.36% | 40.00% | 75.00% | 68.75% | 66.67% | 66.67% | 7.1 s |
| LangGraph Agent | 95.45% | 80.00% | 100.00% | 93.75% | 89.58% | 91.67% | 18.7 s |

Agent Router、Task Count、Retrieval Strategy Accuracy 均为 `91.67%`，Task Coverage 为
`95.83%`，Title Leakage 为 0，Citation Validation 为 100%。Agent 修复了 Fixed Hybrid 在
`AE-017/031` 等问题上的缺论文或错误拒答，证明 Query Decomposition + Coverage Merge 的
Retrieval 增益能够传递到 Answer/Judge 层。

仍需如实保留两个边界：`AE-025` 本应是单论文任务，却被 Planner 错分为 cross-paper；
`AE-032` 虽生成双任务，最终只覆盖一个目标论文，Strict Run 失败。Agent 的质量提升还带来
P50 增加约 11.5 秒、模型调用从 21 次增至 35 次的成本。

### 最终决策

- 这组结果足以支持作品演示从 Corpus v2 切换到 Corpus v3。
- FastAPI、`paper-rag agent` 和 Runtime Factory 默认使用
  `agent_seed_v3_bge_m3_chunking_v1`。
- Planner cache 文件名加入 Corpus 版本，避免跨版本无条件复用计划。
- 不继续针对 AE-025/032 做细碎 Prompt 调参；下一阶段应完成真实 v3 Demo Smoke、文档收尾和
  面试展示材料。

正式产物：

- `evals/baselines/agent_runtime_v2.json|md`
- `evals/diagnostics/agent_runtime_v2.json`
- `scripts/run_agent_evaluation.py`

## 43. 2026-08-19：真实 UI 演示验收与求职材料收尾

### 验收目标

此前 API、前端单测和 Production Build 均已通过，但作品演示还需要验证真实浏览器中的完整任务，
尤其是 SSE Trace、Citation 跳转、Plan/Evidence Tab 和移动端布局。静态测试不能发现模型输出与
UI 交互结合后的问题。

### 真实浏览器结果

在 Corpus v3 上提交 Reflexion 与 Self-Refine 的跨论文比较题，真实链路产生 8 个 SSE 事件：
Planner 路由为 cross-paper 并生成 2 个任务，Coverage Hybrid 召回 10 个 Chunk，Evidence Gate
判定 sufficient，Answer 与 Citation Validation 成功。Report、Evidence、Plan 三个视图均可用；
点击 C1 能切换到 Evidence 并定位 Reflexion 第 2 页。`390 x 844` 移动视口无横向溢出。

### Citation Marker 修复

首次真实回答末尾出现 `[C4][10]`，其中 `[10]` 实际指向已存在的 C10，但旧校验器只扫描标准
`[C#]`，因此错误地把整条回答判为 valid。这不是 UI 样式问题，而是 Citation Contract 漏洞。

本轮在 AnswerGenerator 中加入确定性规范化：`[10]` 或 `[c10]` 只有在当前 Evidence 中确实存在
对应 `C10` 时才改写为 `[C10]`；无法映射的数字标记继续抛出 CitationValidationError。新增测试
覆盖标准引用、可唯一修复的格式和未知数字引用。重启服务后重复同一真实任务，正文所有 Marker
均为合法 `[C#]`，Citation Validation 继续通过。

### 展示材料

- `docs/architecture.md`：增加可渲染的 Mermaid 全链路架构图。
- `docs/demo.md`：五分钟演示步骤、推荐问题、预期 Trace、指标和演示前检查。
- `docs/interview.md`：一分钟介绍、核心决策、常见追问、失败边界和简历项目描述。
- README 增加快速导航，并移除“Agent 对照尚未完成”的过时描述。

## 44. 2026-08-19：v2.0 范围收敛与设计基线

### 进入 v2 的原因

v1.0.0 已证明固定 Corpus 上的 Hybrid Retrieval、Coverage-aware Agent 和三层 Evaluation 能够
工作，但真实使用暴露了三个边界：部分 PDF 文本层会乱码或为空；Corpus 外问题无法主动补充论文；
任务、事件和结果在服务重启后丢失。继续增加 Agent 角色或微调 RRF/MMR 不能解决这些问题。

### v2.0 范围

v2 只包含三个核心 Epic：

1. Parser Quality Router：`pypdf + PyMuPDF + Quality Gate + OCR fallback`，低质量页隔离。
2. Dynamic Corpus Expansion：证据不足时受控搜索 arXiv、下载最多两篇论文、解析并写入独立动态
   Collection，再重新检索。
3. Durable Research Runtime：SQLite 业务库、LangGraph SQLite Checkpointer、持久化 SSE 事件与
   node-boundary Resume。

Open-world Evaluation 作为前三项的贯穿式验收，而不是额外扩张一套在线功能。Dynamic Corpus 如果
没有 In-corpus、Recoverable 和 Unrecoverable 三类对照，就无法证明触发条件、恢复率和拒答边界。
Claim-level Verification 保留为后置增强：只有 Parser、动态 provenance 和持久化记录稳定后才接入，
避免把上游文本或 Retrieval 错误误判成 Claim-Evidence 错误。

### 关键设计决策

- Corpus v3、Collection 和现有 Evaluation 全部冻结；新 Parser 先执行 shadow parse。
- 动态论文写入 `paper_dynamic_bge_m3_chunking_v1`，通过 Federated RRF 与 Curated 结果融合。
- 动态写入后必须失效 Dynamic BM25 snapshot，避免 Dense 与 BM25 看到不同版本的语料。
- 未通过 Parse Quality Gate 的页面不允许 Embedding，只进入 quarantine。
- 应用数据与 LangGraph checkpoint 使用两个 SQLite 文件，通过 Repository Protocol 隔离。
- 重启遗留的 running Task 进入 interrupted，由显式 Resume API 恢复，避免自动重复外部副作用。
- v2 不引入开放互联网爬取、多 Agent、Redis/Celery、长期 Memory 或重新调参。

总体设计和验收指标见 `docs/v2-design.md`；正式决策分别记录在 ADR 003、004、005。实现顺序为
Parser Shadow Mode、Persistent Store、arXiv Metadata Search、Dynamic Ingestion、Agent Loop、
Checkpoint Resume 与 Open-world Evaluation。
